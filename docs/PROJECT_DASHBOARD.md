# Project Dashboard

> **This is a current-state orientation doc. It points to detail, it does not contain it.**
> If a thread needs depth, follow its deep-doc link. If this file grows past ~2 screens, something belongs in a linked doc instead. History lives in the PR trail; do not re-fill it here.

**Last updated:** 2026-08-01 (Sous round 2 design pass issued DRAFT - fix/sous-r2-design)

## Right now

**Service Calendar schedule + phase arc COMPLETE (PRs #391 - #413, 2026-07-10 - 07-12).** The last 48h added a full schedule subsystem to the SC module across seven migrations (sc-13 AWAY loading, sc-15 game_time + day/night, sc-16 AAA parity via `has_homestand_schedule`, sc-17 STL - FL overlay via new `has_schedule_overlay` flag, sc-17b TBJ - FL extension) and two visual amendments (sc-18 indigo game-day corner wedge on sm tiles, sc-19 dark-copper Spring Training wedge + ST pill + chrome rider). The two-flag model (`has_homestand_schedule` classification-driving vs `has_schedule_overlay` informational-only) is orthogonal by design; STL - FL flat-fee daily-service PDC lives under overlay-only, TBJ - FL per-meal PDC extends via sc-17b. Full architecture captured in the new [`modules/SERVICE_CALENDAR.md`](modules/SERVICE_CALENDAR.md); shipped-state + remaining work in [`SC_STATUS.md`](SC_STATUS.md); MLB Stats API capability survey preserved as [`audits/SC_MLB_API_DEPTH_SURVEY_2026-07-12.md`](audits/SC_MLB_API_DEPTH_SURVEY_2026-07-12.md). **Nav-subsystem cold deep-URL fix LIVE (#407)** - Next 16's static-shell prerender at param-carrying URLs was pinning App Router's client state to empty-query state, making `router.push` inert on cold loads; fixed via `export const dynamic = "force-dynamic"` in `src/app/service-calendar/layout.js`. Full read-only investigation preserved as [`audits/SC_NAV_SUBSYSTEM_MAP_2026-07-11.md`](audits/SC_NAV_SUBSYSTEM_MAP_2026-07-11.md). `src/middleware.js` gained the TEST_MODE bypass (double-gated on `VERCEL !== "1"`) that makes the 26-URL Playwright nav-matrix spec possible. **CI rewrite LIVE (#408)** - `.github/workflows/e2e.yml` split into two jobs: matrix (in-runner build + TEST_MODE + `tests/sc-nav-matrix.spec.ts`, on pull_request) and preview-smoke (PR's own Vercel preview URL from the deployment_status event payload). No more hardcoded prod-URL smoke; the pre-#408 shape was "test prod is up," not "test this PR works." **Design-review 4-in-1 fixes LIVE (#409)** - actionable-only counters (X of Y entered excludes off/away from both numerator AND denominator), legend FIGURES row swatches render in full, chrome drill row stays one line + Today pill sheds date, today date badge grows to short pill at 2 digits. **Two silent-gap incidents captured** (sc-16 07-11 pre-DRAFT-rule; sc-17 07-12 through-the-DRAFT-rule via flip-and-merge) - CLAUDE.md gained the migration-gated-DRAFT rule after 07-11; the 07-12 recurrence shows the discipline breaks around a manually-flipped state, motivating the proposed CI hardening (scan PR head for new `docs/migrations/*.sql`, gate merge on explicit "sc-XX applied in Studio: YES" review comment). Not built - documented in [`SC_STATUS.md`](SC_STATUS.md) "Remaining work" awaiting Kevin's go.

KitchFix intranet is live on Vercel; the multi-project Sheets→Postgres migration is well advanced. **Service Calendar Bundle 1 and Bundle 2 are both COMPLETE.** Bundle 1 (PRs #214, #221, #224) made the calendar revenue trustworthy, shipped the contract-revenue fee schedule + admin surface, and added fenced backdate mode for prices + fees. Bundle 2 (PRs #227, #229, #234) added the `active_until` catalog-archive mechanism in three steps: the additive column, the active_until-aware view recreate, and the admin code for archive / reactivate / add-service / add-group. **Service Calendar pricing + fee + catalog-lifecycle admin is now feature-complete.** The **SC season-view redesign is now COMPLETE and LIVE end to end** - the operator-facing Season -> Period -> Day drill, overview *and* drill-in, shipped and polished across a design-review arc (#321-#333). **The SC design audit + journey polish arc is now COMPLETE (PRs #353-#363, 2026-07-08)** - three-section adversarial audit of the Service Calendar journey (overview / drill-in / submissions), 63 findings processed, 2 sev-4 kills (fabricated "ON TRACK" literal; day-notes black hole - typed notes silently discarded with false success, now persisted end-to-end via `sc_day_metadata.day_notes`), money architecture unified on effective-dated view totals returned from `sc-save`, failed states testable via `?debug=failed` on overview + drill scopes, chip pipeline unified on classified status, submission flow hardened (custom-values review, dirty-close guard, review deltas + note, dismissable non-blocking toast, whole-dollar formatting, shared `fmt$`). **Owner Rounds 2-3 complete (#365-#367, SC-064..079, 2026-07-09)** - no-service flows both account shapes (per-meal + MLB homestand for rainouts), entered-only header on first entry, authored notes ledger on `sc_day_note_entries` (author + timestamp per entry, backfilled legacy notes as `—` author), entry-aware homestand classifier (entered non-game days classify Entered), bulk-when-caught-up (Edit today + entered-future gate). **Money-model alignment complete (#368 + sc-8c run 2026-07-09)** - the sc-8b double-discount removed (53 rows deleted), all `actual_revenue` history self-healed via the effective-dated view's `COALESCE(pr_act, pr_proj)` fallback, [`SC_MONEY_MODEL.md`](SC_MONEY_MODEL.md) is the canonical money authority (wins conflicts). **Cleanup phase COMPLETE (#370-#373)** - C3 docs + audit archive + em-dash sweep; C2 dead reads + CSS tokenization + `.sc-tactile` family; C1a glyph + format + legend + `<ProgressBar>` extractions (resolved SC-035); C1b dirty-guard value snapshot + save AbortController + focus placement + success `role="status"` + midnight-stale `isPast`. **Operator Excel export LIVE (#374)** - `GET /api/service-calendar/export` streams a three-sheet workbook (Summary K1/K2/K3 + Daily detail + Notes ledger with L1 BY WEEK / L2 WEEKDAY AVERAGES / L3 SERVICE PERFORMANCE), effective-dated dollars from `sc_daily_revenue`, DRAFT stamp while incomplete, TXR-TX-V revenue-external note, J2 icon-only square in the chrome. **F1 History LIVE (#375)** - unified DayDetail Activity ledger merging authored notes + actuals-edit trail (`sc_daily_actuals_history` trigger, server-derived author) with M2 chips + a mark-no-service system row; export gained a Changes sheet (L4, counts only) + a BY OPPONENT rollup on homestand-fee accounts (L5). **Export period-resolution hotfix LIVE (#376)** - the `sc_day_metadata.period` bare-numeric-string incident, missing-fee honesty row, cell-format polish. **F2 per-user home account LIVE (#377)** - role-driven mount landing via `computeInitialView({..., hasHomeAccount})`: floor + `user_accounts` row lands on their account + current period + today focused; leadership + floor-without-home + unmapped fall through to Season overview (picker). URL account/scope + admin gate + account switcher untouched. **F3 save-queue resilience LIVE (#378)** - a localStorage-backed queue enqueues sc-submit-day network failures per (account, date), replays with 5s → 15s → 45s → 2m → 5m backoff + immediate `online` attempts + mount replay; the tile carries an N1 SYNCING badge while queued, DayDetail closes cleanly to the grid (no premature success screen); last-write-wins replay per the R-D ruling (F1 Activity ledger records both edits). `beforeunload` warns while queue is non-empty; multi-tab lock (60s TTL) prevents double-replay. **P1 trust + polish shipping** - one completeness rule (`entered || no-service`) across MonthCard/PeriodCard/FullSeasonCard/workspace/export (kills the CIN-OH June 29/30 vs 30/30 divergence); top-nav Service Calendar re-runs the F2 landing so a fresh click from any drill lands on Season overview (leadership) or the current period + today focused (floor+home); R1a-navy extras band restyled as the sc-day-group-header's structural sibling (3px navy left accent, white ground); R4b workspace ghost tiles for in-period dates without metadata (dashed border, muted numeral, inert to keyboard + counts). **F6 phase foundation SHIPPED** - additive `sc_phase_calendar(account_key, phase, start_date, end_date)` migration (`sc-11`) with 48 seeded rows across the 5 PDC accounts (CIN-AZ 13 / TXR-AZ 10 / TBR-FL 9 / TBJ-FL 8 / STL-FL 8, TBJ + STL Kevin-approved 2026-07-10 as TBR-peer arcs); operator export gains a BY PHASE table on year scope for PDC accounts with seeded rows (`Phase · Dates · Proj $ · Actual $ · Meals`), placed after SERVICE PERFORMANCE and before BY PERIOD; safe-degradation on pre-migration prod - loader swallows the missing-table error, the block is simply absent. Phase names stored as-recorded per the SC_PDC_PHASES.md Stage 2 canonical-vocab deferral; date-range join reads the same effective-dated per-day totals the rest of the sheet aggregates from (no second money path). **P2 notes UX SHIPPED** - the DayDetail note composer becomes the primary "Note for this save" ride-along field above the action bar; the operator's draft joins the `sc-submit-day` payload as `rideNote`, the server appends it via the existing authored-note path (server-derived author) AFTER the actuals save succeeds, a post-save note failure surfaces as `noteFailed:true` with a partial toast ("Saved - note couldn't post, use Add note") that never lies about a clean success. The review overlay shows a "Note riding this save" block (sc-tint ground, Edit + Remove actions) that replaces the retired "Latest note" history echo entirely; a subordinated "+ Add a note without saving" link below the action bar exposes the same Add note wire for the standalone-post edge case. DaySquare gains an 11px chat-bubble corner glyph at ~50% opacity on days carrying at least one NOTE ledger entry (NOTE-only per Kevin's Q-b ruling - history rows never count), fed by `hasNoteEntries` on the year-summary payload and `day.noteEntries.length > 0` on the drill-in workspace; legend popup picks up a "Has notes" row in Calendar context. Bulk endpoints do NOT accept `rideNote` (v1 fence - a single note across N days would blur which day it was meant for). Rider (owner ruling b): BY PHASE export table splits its hybrid Meals column into a symmetric `Proj meals · Actual meals` pair so partial phases read honestly by construction and both columns foot to the year TOTAL MEALS headlines. Remaining SC work stays parked (Admin Dashboard, Fun Money Tracker, Close Day button). The **OPD Command rebuild** PRs A/B/C plus polish are merged; the **OPD authoring editor** is **COMPLETE / LIVE** - in-app editing now publishes through a per-doc PR that auto-merges past the Playwright gate, with the B2 Action projecting + re-embedding on merge (#211, #213, #226, #232, #233). **News admin + NewsFeed 2.0** plus post reactions are live (#210, #228).

## Active threads

| Thread | Status | Next step | Deep doc |
|---|---|---|---|
| Service Calendar - Redesign Program W0-W9 + V3 arc | **V3 OVERVIEW + DRILL PHASE 1 COMPLETE** - OV: #471 + #472 + #473 + #474 + #476 all MERGED 2026-07-19. Drill P1: #478 (D week grouping) + #480 (C rail) + #479 (A chrome + HF-1..7) + #481 (B visual + DEN-1..3) all MERGED 2026-07-20. Month-drill regrouped to calendar-week + period prefix (fixes the '4 of 9' mis-total; period view unchanged via groupBy branch); single-row header with overview parity + Today-group; legend state spine unified (Non Game day kept distinct per owner guardrail); airplane retired, away=purple; tile density 68px + 8px radius (`--sc2-radius-tile`); entry CTAs in rail. Owner device-pass + merge 2026-07-20. | Drill Phase 2 - DayDetail / DayEntryV2 modal visual pass (highest-traffic operator surface, own render, deferred out of P1 scope; see [V3_HOPPER.md](V3_HOPPER.md) `next-season`). W9 PR 2/2 (demolition + `sc-v2-complete` tag) still parked on Kevin's explicit soak go. Instrumentation left in for the F3-redux gate cell (`data-f3-target` attribute on `.sc-rail-scroll` at `SeasonRail.js` + `OpsRail.js`) - harmless; slated for removal in the demolition PR. DP1-20 FIGURES key drill-only scoping deferred (needs StateLegend->LegendInfoPopup scope plumbing). | [SC_REDESIGN_PROGRAM_SCOPE.md](SC_REDESIGN_PROGRAM_SCOPE.md) §18 · [design/V3_OVERVIEW_SPEC.md](design/V3_OVERVIEW_SPEC.md) §v4 · [V3_HOPPER.md](V3_HOPPER.md) · [BUILD_ACCURACY_PROTOCOL.md](BUILD_ACCURACY_PROTOCOL.md) · [SC_ENTRY_V2_SCOPE.md](SC_ENTRY_V2_SCOPE.md) |
| Service Calendar - season-view redesign + drill-in alignment | **COMPLETE / LIVE.** Overview levels + the drill-in (Period & Month workspaces, DayDetail) are aligned to the design-system bar and shipped across a design-review arc (#321-#333): consolidated chrome header, the colors data-fix, WAI-ARIA keyboard grid, month-scoped drill-in (un-deprecated the month view), meals-first tiles, half-height totals strip, exception chips + next-needing-entry iterator, projection pill, today frame - plus a nav-refresh + account-switch bug cluster (#332/#333). A second arc (#344-#351) rebuilt the **DayDetail day-entry modal** (scoreboard header + live total + clubhouse cards + hybrid review/success + tightened rows + Match footer), shipped the **momentum submission toast**, ran the **overview polish set** (bulk-confirm, styled Cancel, nav-reset, entered/projected revenue, MLB twin-stat header, exact-amount week cards, real-status today tiles, sun/moon glyphs, MLB opponent pills), restyled the **Season Summary into the capstone card**, and removed the **Need Help FAB** intranet-wide. A third arc - the **SC design audit + journey polish** (#353-#363) - closed 63 audit findings across the overview / drill-in / submissions journey, killing 2 sev-4s, unifying money on effective-dated view totals, wiring failed-state tests, and hardening the submission flow. **Owner Rounds 2-3 (#365-#367) + money-model alignment (#368)** land the entered-only header, authored notes ledger, entry-aware homestand classifier, MLB rainout no-service flow, and remove the sc-8b double-discount so `actual_revenue` reads the true per-meal invoice rate. Overview + drill-in + day-entry share one clubhouse-grade language + one truthful money source. | 4-step next sequence: (1) **feature queue on roadmap v2** - **P1 + P1.1 + F6 + P2 shipped**; queue -> **P3-A gate** -> **P3** next; **F4 notifications PARKED pending a dedicated conversation; F5 projection revision REMOVED by owner ruling 2026-07-09** (client-confirmed future numbers flow through the bulk future-actuals path by design, not through a separate projection edit surface); (2) runtime checklist (~10 min, non-blocking) - `?debug=failed` on overview + a drill, offline stale-data test (SC-014), as-of pill over bright hero (SC-020), <640px urgency check (SC-036 - expected to promote to a real finding); (3) **overall webapp function review → pre-launch** (SC still dev-gated behind Coming Soon; SC-011 zoom discussion parked, revisit here); (4) any deferred cleanup polish (SC-016 skeleton parity + partial-fail path are the only deferred items). | [SC_MONEY_MODEL.md](SC_MONEY_MODEL.md) · [DESIGN_AUDIT_LEDGER.md](DESIGN_AUDIT_LEDGER.md) · [SC_MONEY_ALIGNMENT_REPORT.md](SC_MONEY_ALIGNMENT_REPORT.md) · [SC_REDESIGN_SPEC.md](SC_REDESIGN_SPEC.md) §13 · [SC_DRILLIN_ALIGNMENT_AUDIT_CC.md](SC_DRILLIN_ALIGNMENT_AUDIT_CC.md) · [HANDOFF_CHAT.md](archive/handoffs/HANDOFF_CHAT.md) · [HANDOFF_CC.md](archive/handoffs/HANDOFF_CC.md) · [SC_DRILLDOWN_DECISIONS.md](SC_DRILLDOWN_DECISIONS.md) |
| Service Calendar - Bundle 1 | **COMPLETE**. Stage 1 calendar accuracy + OperationalView module + isFeeAccount structural split (PR #214). Stage 2 `sc_fee_schedule` + admin fee surface + seed of the 5 locked 2026 fees (PR #221). Stage 3 fenced backdate mode in both PriceEditPanel and FeeEditPanel with recompute-range warning (PR #224). The two-revenue-layer architecture (per-meal/operational reads `sc_daily_revenue`; contract revenue lives in admin + future KPI dashboard) is now load-bearing in code. | None - bundle closed. | [SC_CONTRACT_BILLING_SUMMARY.md](SC_CONTRACT_BILLING_SUMMARY.md) (the contract bible) · [ACCOUNT_SERVICES_BRIEF.md](ACCOUNT_SERVICES_BRIEF.md) |
| Service Calendar - Bundle 2 (catalog lifecycle) | **COMPLETE**. sc-6a `active_until DATE` column on `sc_services` + `sc_service_groups` (PR #227). sc-6b view recreate to make `sc_daily_revenue` + `sc_month_summary` active_until-aware (PR #229). sc-6c admin code for archive / reactivate / add-service / add-group with backdate fence + group blast-radius confirm (PR #234). **Locked archive model** (canonical reference for any future catalog work): archive = set `active_until` to a DATE (NULL = active forever; a date = active through that day inclusive; days strictly after are archived). Reactivate = set `active_until` back to NULL. Backdated / today / future-scheduled archive are all legitimate. The pre-existing `active` BOOLEAN is a UI-only toggle (views do NOT filter on it) and stays untouched by Bundle 2. `deleted_at` stays a dormant unused hard-delete path. Every catalog write pairs an `sc_config_changelog` row (entity_type `service` or `group`). | None - bundle closed. | (no repo deep-doc; locked model documented in sc-6a + sc-6c PR descriptions) |
| Service Calendar - parked items | Future deliverables held until prioritized: **Admin Dashboard** (cross-account ops overview surface), **Fun Money Tracker** (non-revenue catalog tracking surface), **Close Day button** (per-day lock / confirmation UX). Plus an eventual design-polish pass across the SC admin once these land. | Pick one when Kevin commits to it. | (not scoped) |
| KPI dashboard | Parked. `sc_fee_schedule` is its eventual first data source but the dashboard itself is not being built toward right now. | Reintroduce as its own explicit thread when Kevin commits to it. | (parked) |
| OPD Command rebuild (was Build Dashboard) | PR sequence A/B/C MERGED (#194/#195/#197). Multiple cockpit polish passes landed (#198/#200/#201/#206/#207/#208). **PR D (governance surfaces: access-tier editor + issues triage) status TBD - Kevin to confirm shipped / descoped / pending.** | Confirm PR D scope or close out the rebuild thread. | [BUILD_DASHBOARD_AUDIT_CC.md](opd/BUILD_DASHBOARD_AUDIT_CC.md) · [BUILD_DASHBOARD_ENGINE_MAP_CC.md](opd/BUILD_DASHBOARD_ENGINE_MAP_CC.md) |
| OPD authoring editor | **COMPLETE / LIVE**. Editor surface + GitHub MDX read (#211); commit path (#213); MDX-source no-store (#226); commit-mdx honest GitHub write-error surfacing (#232 - exposed that ruleset 16364953's `pull_request` rule was blocking direct-to-main PUTs, the root cause of the long save-409 saga). Publish flow now writes to `opd-edit/<id>`, opens (or reuses) a PR to main, and enables GitHub native auto-merge (squash); the B2 Action projects + re-embeds on merge (#233). Proven end-to-end live. | SousAI live retrieval wiring (Phase B) + the deferred PR D (access-tier editor + issues triage) when prioritized. | (no repo deep-doc yet) |
| OPD/Playbook Phase A (engine) | DONE. A1-A7 merged. Document Format Standard arc layered on top (PRs #178/#180/#182/#183/#184/#187). Auto-projection + SousAI re-embed run as a GitHub Action on content commits (#222, node-22 bump in #223); the cockpit authoring publish-flow (#233) closes the loop by routing cockpit edits through the same PR + check gate code uses. | Phase B (SousAI live retrieval caller + hero search, reader reading-experience UX, shelf taxonomy). | [PHASE_A_PR1_HANDOFF.md](opd/foundation/PHASE_A_PR1_HANDOFF.md) · [PROJECTION_DRYRUN.md](opd/foundation/PROJECTION_DRYRUN.md) · [PLAYBOOK_ENGINE_AUDIT.md](opd/PLAYBOOK_ENGINE_AUDIT.md) |
| OPD content review | Reviewer packets out: Britt (culinary), Counsel (policy), Finance (pay bands + permits). **Markups status since 2026-06-17 - Kevin to update.** | Await markups; record approvals to MDX frontmatter; flip In Build → Live on sign-off. | tracked by Kevin |
| News admin + NewsFeed 2.0 | LIVE. Image upload + reading overlay (#210). Post reactions (like / fire / cheer / heart) + home rename added (#228). | Monitor in production; future iteration on editorial workflow as needed. | (no repo deep-doc yet) |
| Sheets → Postgres migration | Stage 1 in flight; modules 1-3 cut over; Service Calendar = 4th cut-over (dev-only). Finance stack (Project 3) scoped (12 PRs). | Per `MIGRATION_STATUS.md` - confirm next module with Kevin. | [MIGRATION_STATUS.md](MIGRATION_STATUS.md) · [FINANCE_STACK_PLAN.md](FINANCE_STACK_PLAN.md) · [FINANCE_STACK_AUDIT.md](FINANCE_STACK_AUDIT.md) · [MIGRATION_APPROACH.md](MIGRATION_APPROACH.md) |
| Open PRs awaiting decision | PR #140 (m6 post-fix audit reminder for June 19). | Kevin reviews + merges or closes. | the PR itself |
| Minor SC backlog (not urgent) | **TBJ - NY Snack + Shake services** are `active=false` on `sc_services` (deactivated via the retired gear on 2026-06-15) but `active_until=NULL`. With sc-6c merged they now render as normal active rows in the admin (not archived) because the views and the new UI both read `active_until`, not `active`. Optional cleanup: properly archive them via the new UI (sets `active_until`) or leave as-is. | Defer until somebody is in the admin for TBJ - NY anyway. | n/a |
| Stale branches to clean up | None. | n/a | n/a |

## Recently done

- **SOUS ROUND 2 DESIGN PASS ISSUED DRAFT (2026-08-01) - `fix/sous-r2-design`.** Follows the round-1 hotfix (PR #587 merged). Four picks land: **QA-2** (docked header - question docks INTO the answer card as a tinted header with teal micro-bar + status pill right-aligned on the same line; interior depth via `.sa-well` for meta rows + `.sa-source-row` contained sources; status companion returns at the correct placement inside the tool-trail well, never touching the rail), **L-A** (48px 1C lockup mark left of the landing heading + subcopy; wake moves here from the hero which now holds rest drift only; **idle flourish system** - new `SousLockup` client component owns a single timer that fires a random flourish (check / one-leg / glint) every 20-45s with a first-fire bound of 12s post-wake; suppressed under reduced-motion, paused on tab hidden), **icon tiles** (28px radius-8 module-tinted tile per briefing row), **depth tokens** (`.sa-well` + `.sa-source-row` formalized as shared classes, apply to page + panel identically). Both variants covered by construction (`SousSurface` serves both). No source-code changes beyond the four items; no migration. Full delivery log entry in [`SOUS_REDESIGN_MASTER.md`](SOUS_REDESIGN_MASTER.md).
- **SOUS R2 HOTFIX MERGED (2026-08-01) - PR #587.** Six mechanical fixes + I5 panel starters + panel limits copy. Highlights: `sources: [object Object]` normalized (I2 hydration exposed the stringify path), tool-line label echo fixed (`summarizeToolStart` covers all 13 tools, fallback becomes `"running"`, streaming companion generic), rail-corner mark artifact removed (status companion deferred to correct placement in the r2 design pass), nav-offset fit math (`--kf-topnav-h` on `:root` in `TopNav.css`; `.sa-page` becomes `calc(100dvh - var(--kf-topnav-h))`), Spend example chip shortened, hero mark centering (wrap+absolute mark), per-host starter map for the panel (`PANEL_HOST_STARTERS` + `starterSetForHost` resolver).
- **SOUS PR A POLISH PASS ISSUED DRAFT (2026-08-01) - `fix/sous-pra-v1-polish`.** Nine logical commits landing U1-U12 + I1-I4 ratified 2026-08-01 against the visual + code-side review pair (`docs/SOUS_V1_CODE_REVIEW_2026-08-01.md`). No migration - Part 6's pre-flight probe found the four target vendor aliases already exist in `vendor_aliases` and every target row in `ai_line_items` resolves via `vendor_id` (pr-8-1's SET NOT NULL FK); code-only fold of `spend_top_vendors` to aggregate by `vendor_id` + shared `countYtdCanonicalVendors` helper the first-run Spend chip consumes. Parity [ran]: chip 38 == tool 38. Fifteen files changed across parts 1-9 - highlights: V2 simplified briefing (four-card grid → one elevated card with rows), elevation tokens `--elev-1/2/3` scoped via `:where(.sa-page, .pb-sous-panel)`, treatment-A answer card, composer 52px + Flame focus ring + real disabled + clear-on-submit, SousMark component per `docs/SOUS_MARK_SPEC.md` (2 colorways / 6 states / wake / animationiteration handoff / attend via `:has()`) deployed at hero+panel-band+status-companion+nav+favicon, mdLite extension `##`/`###`/`---`, done envelope carrying `flags` + `truncated`, reason chip below PARTIAL pill, ERROR `role="alert"` + `#DC2626`, live region narrowed, client-side `FreshnessChip` + `pgLiveNow()` at every tool `loaded` field (no raw ISO leaves the tool layer), panel I1 empty state (two modes), I2 source-card title hydration, focus-visible for 7 bare classes, reduced-motion broadened to transitions. Full delivery log entry in [`SOUS_REDESIGN_MASTER.md`](SOUS_REDESIGN_MASTER.md) v1.4.
- **SC DRILL PHASE 1 COMPLETE (2026-07-20) - #478 (D week grouping) + #480 (C rail) + #479 (A chrome + HF-1..7) + #481 (B visual + DEN-1..3) all MERGED.** Month-drill regrouped to calendar-week rows with period prefix (`P7` / `P7-8`), fixing the `4 of 9` mis-total that surfaced when the fiscal-week bucket straddled a month boundary; period view unchanged via a `groupBy` branch that keeps fiscal-week for period scope. Header collapsed to a single navy ribbon with overview parity: Season back + period stepper + Today-group folded into the ribbon; ChromeBar suppresses on scv2 non-admin (v1 + admin still use it); overview + drill share the bordered `--boxed` Today-group with a working ⊙ Today button (drill jumps to period/month via URL router; overview scrolls the current-month card into view + pulses it via `.sc-season-month-card--today-pulse`, hidden if viewing a non-current year). Owner device-pass surfaced 7 header hotfixes (HF-1 white-on-white drill-nav buttons; HF-2 phase pill removal; HF-3 period label to caption tier; HF-4 date range hidden; HF-5 export in ribbon-right final child + rail dedup; HF-6 account selector shrink + on-navy retint; HF-7 overview Today-group parity + pulse). Legend state spine unified (`Entered · Needs entry · Overdue · Upcoming · Today` identical across account arrays); HOMESTAND `off` "Non Game day" kept distinct per the guardrail (semantically different from per-meal off). Airplane retired at tile + swatch; away tiles now purple. Tile density fix DEN-1..3: `.scv2 .sc-workspace-grid .sc-daysq--lg` (specificity bumped from 0,2,0 to 0,3,0 to beat the periodWorkspace.css:570 96px override that was silently winning the cascade) lands 68px min-height + 8px radius via new `--sc2-radius-tile` token. Entry CTAs moved to rail (primary + Enter-today + Bulk-entry secondary). **DayDetail modal visual pass DEFERRED to Phase 2** (highest-traffic operator surface, own render).
- **PRs #365-#368 MERGED (2026-07-09) - Owner Rounds 2-3 + money-model alignment.** Round 2 (#365 behavioral + #366 visual) closed SC-064..074: `~` prefix on projected header + entered-only hero on first entry (SC-072); server-authoritative saved totals for mark-no-service (SC-066) counting complete via widened aggregate `hasActuals || status=='no-service'`; delta-chip amber-under semantics + weight-only magnitude (SC-064); dirty-close guard with `useImperativeHandle` (SC-063 stayed OK); empty-input phantom-delta fix (SC-071); bulk-when-caught-up (SC-069); C2 stacked "of" lockup (SC-067); MiLB tile PDC-anatomy with top-row sun/moon (SC-070); toast timing 3.5s + outside-click dismiss (SC-068); MLB week-cards removed (SC-073); F3 elevation on expanders (SC-074); + a Q5 rename ("cost basis" → "post-SF invoice rate"). Round 3 (#367) closed SC-075..079: G1 extras band, H2 Match-projections pill, MLB Mark-no-service (SC-077 rainouts), entry-aware homestand classifier (SC-078 - entered non-game days classify Entered), and the sev-3 authored notes ledger on `sc_day_note_entries` with server-derived author + timestamp + backfill from `sc_day_metadata.notes` (SC-079). The alignment PR (#368) landed [`SC_MONEY_MODEL.md`](SC_MONEY_MODEL.md) as the canonical money doc and ran `sc-8c` to delete 53 double-discounted `'actual'`-kind rows from `sc_service_prices`; `sc_daily_revenue.actual_revenue` now reads the true per-meal invoice rate (30% correction on CIN-AZ, 20% on TXR-AZ, 25% on TBR-FL MiLB) via the view's `COALESCE(pr_act, pr_proj)` fallback - all history self-heals through the effective-dated view. Full three-way reconstruction of how the money model got here in [`SC_MONEY_ALIGNMENT_REPORT.md`](SC_MONEY_ALIGNMENT_REPORT.md). Doc drift trued up across ACCOUNT_SERVICES_BRIEF + SC_SPREADSHEET_MAPPING + bible + BUSINESS_NOTES + dataStore comments; GOTCHAS gained three entries (shifted-input backfill trap; out-of-band Supabase corrections; CPI via admin backdate not sheets).
- **PRs #353-#363 MERGED (2026-07-08) - SC design audit + journey polish arc.** Three-section adversarial audit of the Service Calendar journey (overview / drill-in / submissions), 63 findings processed. **2 sev-4 kills:** the fabricated "ON TRACK" literal (killed at the source); day-notes black hole - the notes textarea was wired to nothing, typed notes silently discarded while the toast confirmed success, `day_notes` column read but never written (now persisted end-to-end via `sc_day_metadata.day_notes` with a two-step upsert that preserves the row's context columns; DayDetail prefills on open and includes notes in the save payload). **Money architecture unified:** `sc-submit-day` now reads `sc_daily_revenue` AFTER the write and returns `savedRevenue` / `savedMeals` from the effective-dated view (LATERAL price join). Toast + review + tile + drill rail + bulk-review header all echo one truth; killed the current-catalog-vs-effective-dated drift class that was surfacing as $2,708/$2,709 rail-vs-modal and $21,483/$21,490 toast-vs-review. **Failed states testable:** `loadState` wired through overview AND drill scopes with a `?debug=failed` dev hook (isDev-gated). **Chip pipeline unified** on classified status (killed phantom counts from aggregate reading raw `isPast/isLocked` while classifier excluded them via `no-service`); **fee ruling split:** STL-FL = real urgency end-to-end (falls through per-meal branch by design), MLB homestand = pure schedule view (chips + amber removed by ruling). **Submission flow hardened:** custom-values bulk path now has a review step matching match-projections anatomy (SC-062); dirty-close guard on Esc / backdrop / X / Cancel via `useImperativeHandle` gate with a "Keep editing" / "Discard" confirm (SC-063); review-overlay carries the same delta chips as entry plus the day-note in quotes or an explicit "No note" muted state (SC-054 + SC-053); toast becomes a `<button>` with click-to-dismiss + Enter/Space + `pointer-events: none` on container so grid tiles beneath the dead zone stay clickable (SC-060); whole-dollar toast (SC-057); shared `season/format.js` `fmt$` util (replaced three drifted inline copies); PDC segment dedupe in group price line (SC-058); contrast fixes to WCAG AA (SC-059/061 - `--text-subtle` -> `--text-muted` at 4 sites, delta amber -> `--status-needs-fg` at 7.72:1). `docs/DESIGN_AUDIT_LEDGER.md` = the full audit record (63 rows, statuses, rulings, PR stamps).
- **PRs #344-#351 MERGED (2026-07-07) - SC DayDetail modal redesign + overview polish arc.** The day-entry modal was rebuilt into the "A+C hybrid": a navy **scoreboard header** carrying the live running total + record status with the footer trimmed to an action bar (#344); the **clubhouse card system** - per-row match-✓ removed (green input owns "entered"), tightened balanced rows, full-width show-more, sealed subtotals, plus the **review + success overlays** brought into the same scoreboard/card language (#346); and larger anchoring group headers + PDC account-segment context + Match-projections restyled from a floating button to a full-width footer row (#351). A **momentum submission toast** shipped scoped to the four SC submission calls - rotating clubhouse-voiced headlines (no emojis) + period-progress milestones + a filling completion bar (#345), later recentered over the calendar grid (#350). Overview polishes: bulk "All match projections" now routes through a confirm/review + a styled bulk Cancel button + Service-nav-resets-to-overview-preserving-account (#347); the metrics band reads **entered / projected** revenue + the MLB/fee header rebuilt as the **game-days + meal-counts twin-stat** (one visual family with per-meal) + week subtotals as exact-amount mini-cards (#348); today tiles now reflect **real status** instead of a forced-green override + day/night became **sun/moon glyphs** + MLB opponent pills (#349). The **Season Summary** was restyled into the "capstone card" - a soft-slate header band at period-card height + divided stats + inline completion bar - and the global **HelpFAB ("Need Help?") was removed intranet-wide** (#350). DayDetail redesign + SC overview polish set are both COMPLETE.
- **PRs #321-#333 MERGED (2026-07-02 → 07-06) - SC drill-in redesign arc + bug cluster.** Design arc: workspace alignment + today-rail merge (#321/#322), consolidated chrome header (#323), colors data-fix (#324 - the month endpoint omitted the `status` field so the drill-in grid rendered every cell neutral; `classify()` extracted to shared + run on the month path), WAI-ARIA roving-tabindex keyboard grid (#325), month-scoped drill-in (#326 - clicking a month opens `?month=` not the containing period; un-deprecates the month view), meals-first tiles + Season-link chrome + month week-totals drop (#327), totals card → half-height strip, entered-led, "to go" removed (#328), exception chips + next-needing-entry iterator + projection pill (#329), nav-refresh loading affordance + floor-redirect latch (#330), chrome no-wrap + entered-card layout + today frame+chip (#331). Bug cluster: account-switch blank grid (#332 - stale-closure/missing-dep in the drill-in fetch effects) and dead drill-in nav (#333 - the #330 `<Suspense>` boundary swallowed `useSearchParams` client-nav updates; removed). Drill-in stable across period/month/current/upcoming.
- **PR #313 MERGED (2026-07-01)** - SC drill-in token cleanup (first pass of the drill-in design-system alignment). `PeriodWorkspace.js` progress-bar + delta colors now point at tokens (`--status-needs-strong` / `--accent-sc` / `--text-success` / `--accent-text`); the dead `GREEN / RED / AMBER` constants in `DayDetail.js` removed. PeriodWorkspace now carries zero raw color; DayDetail's coaching-banner hex is the next target.
- **PR #233 MERGED (2026-06-22)** - OPD authoring publish-through-the-gate. Save commits to `opd-edit/<id>` (created from main HEAD, reused on re-save), opens or reuses a PR to main, and enables GitHub native auto-merge (squash) so the PR self-merges once the Playwright check passes; B2 then projects + re-embeds on merge. Editor shows "Submitted for publish - PR #N" + clickable link; cockpit pending-publish indicator lists open `opd-edit/*` PRs so any stalled / failed PR is visible. Branch-protection ruleset relaxed in parallel: `strict_required_status_checks_policy` (require-branches-up-to-date) turned OFF so an opd-edit PR can auto-merge even if main advances under it; PR-required + Playwright required-check remain ON. Proven live end-to-end with no manual merge. OPD authoring editor thread now COMPLETE.
- **PR #234 MERGED (2026-06-22)** - SC Bundle 2 Step 3 (sc-6c): catalog lifecycle admin. New orchestrator functions (`archiveService`, `reactivateService`, `archiveServiceGroup`, `reactivateServiceGroup`, `addServiceWithAudit`, `addServiceGroup`), six isScAdmin-gated route actions, five new admin panels. Archive sets `active_until` (Today/Future/Backdate fence, backdate carries the calendar-day recompute warning). Group archive carries a blast-radius warning naming the count of services affected plus a confirm checkbox. Archived services render visible-but-marked (greyed badge + Reactivate button). Every write pairs an `sc_config_changelog` row. The Accepted Stage 2 Tradeoff is now LIFTED. Bundle 2 complete.
- PR #232 MERGED (2026-06-19) - OPD commit-mdx: surface GitHub write error instead of mislabeling as stale (the 409 path now exposes the real error to the editor UI).
- **PR #229 MERGED (2026-06-19)** - SC Bundle 2 Step 2 (sc-6b): DROP + CREATE of `sc_daily_revenue` + `sc_month_summary` with `active_until`-aware catalog JOINs `(active_until IS NULL OR service_date <= active_until)` alongside the existing `deleted_at IS NULL` filter. Byte-level diff vs sc-1b confirms only the two JOIN ON-clauses changed; `sc_month_summary` body byte-identical. Reversibility: the sc-6b file inlines the sc-1b definitions verbatim for paste-and-run rollback. Verified live via positive test (future days excluded, history preserved) and a sanity revenue read across multiple accounts.
- PR #228 MERGED (2026-06-19) - News post reactions (like / fire / cheer / heart) + home rename (dashboard → home).
- **PR #227 MERGED (2026-06-19)** - SC Bundle 2 Step 1 (sc-6a): nullable `active_until DATE` column on `sc_services` + `sc_service_groups`. Additive, no backfill, no view changes, no GRANT change. The column is dormant until sc-6b reads it. Header documents the three-way clarification: `active` BOOLEAN is a UI toggle (views do not filter on it); `active_until` is the billing-relevant archive; `deleted_at` is a dormant hard-delete escape hatch.
- PR #226 MERGED (2026-06-19) - OPD authoring: mdx-source `no-store` (fixes stale-SHA 409) + reload recovery + 409 SHA diagnostic.
- **PR #225 MERGED (2026-06-19)** - SC seed: `scripts/_seed_sc_from_xlsx.mjs` price upsert switched to `ignoreDuplicates: true` so re-imports cannot clobber manual SQL price corrections (the 2026-06-16 CIN-AZ incident). Services/groups/projections/actuals/metadata keep ON CONFLICT DO UPDATE - re-importing those is the intended behavior. Clean re-apply of the superseded `fix/sc-seed-preserve-manual-prices` branch.
- **PR #224 MERGED (2026-06-19)** - SC Bundle 1 Stage 3: fenced "Backdate" radio in both PriceEditPanel and FeeEditPanel. Server `allowBackdate` opt-in skips the today-or-future floor; still validates `>= 2024-01-01` (typo floor) and `<= today`. Price warning names the calendar-day span and is explicit that the system has no record of which days have been invoiced; fee warning is a data-fact statement about the contract-revenue history (no claim about the not-yet-built KPI dashboard). Bundle 1 is now complete.
- PR #223 MERGED (2026-06-19) - OPD autoprojection: bump node 20 → 22 (supabase needs native websocket).
- PR #222 MERGED (2026-06-19) - OPD: GitHub Action for auto-projection + SousAI re-embed on content commits.
- **PR #221 MERGED (2026-06-19)** - SC Bundle 1 Stage 2: `sc_fee_schedule` table + admin fee surface (`FeeAccountEditor` + `FeeEditPanel` + new sc-admin-fee-* route actions) + idempotent seed of the 5 locked 2026 fees (CIN-OH $362,500 / STL-MO $473,000 / TXR-TX-H $604,032 / TXR-TX-V $0 covered-by-H / STL-FL $1,400,000). Insert-only GRANTs on the fee table; bundle invariant CHECK enforces amount=0 when covered_by is set. Architecture lock preserved: the Service Calendar UI does not import or read `sc_fee_schedule`.
- PR #220 MERGED (2026-06-19) - OPD projection: use TRUNCATE instead of DELETE in replace functions.
- **PR #217 MERGED (2026-06-19)** - Contract bible: `RESOLVED BILLING DECISIONS` section added to `SC_CONTRACT_BILLING_SUMMARY.md` with the five locked fee values. Authoritative billing reference for Bundle 1 Stage 2/3.
- **PR #214 MERGED (2026-06-19)** - SC Bundle 1 Stage 1: route forwards view-sourced revenue + ServiceCalendar.js reads it (kills the mid-period price drift); `isFeeAccount` split into structural predicates; `OperationalView.js` module owns the operational-only display (no fmt$ import = no $ can leak); STL-FL promoted to fee-display structurally.
- (earlier entries through PR #209 captured in the prior dashboard revision)

## Pointers

- [SC_CONTRACT_BILLING_SUMMARY.md](SC_CONTRACT_BILLING_SUMMARY.md) - **contract bible** - authoritative billing reference (RESOLVED BILLING DECISIONS at top)
- [ARCHITECTURE.md](ARCHITECTURE.md) - Sheets + PG dual data layer + auth boundary + module map; includes the OPD source-of-truth boundary (MDX-authored vs Postgres overlay)
- [CONVENTIONS.md](CONVENTIONS.md) - action-dispatch APIs, CSS prefixes, sheet column conventions
- [GOTCHAS.md](GOTCHAS.md) - hard-won lessons; read before debugging anything that smells familiar
- [HOW_WE_WORK.md](HOW_WE_WORK.md) - orientation primer for new sessions
- [MIGRATION_STATUS.md](MIGRATION_STATUS.md) - canonical current-state of the Sheets→PG migration
- [BUSINESS_NOTES.md](BUSINESS_NOTES.md) - living reference for domain rules + account-specific quirks
