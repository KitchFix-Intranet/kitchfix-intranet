# KitchFix Migration Project Dashboard

**Last updated:** 2026-05-28 (end-of-session, post-Project-3-planning). **Project 3 = finance stack migration is fully scoped and ready to execute.** 12-PR plan landed in PR #84 (`docs/FINANCE_STACK_PLAN.md`, 1087 lines) building on the 1294-line code audit in PR #83 (`docs/FINANCE_STACK_AUDIT.md`). Migration order: dataStore.js split first (Module 4 of Project 3), then vendor (Module 5), invoice (Module 6), Smart Inventory lift-and-shift (Module 7), Railway cron last (Module 8). Total estimate: 105-145 hours. All 17 schema/business/scope decisions locked; all 6 Section 8 open items resolved with Kevin's answers. **Module 3 (People Portal submissions) CUT OVER 2026-05-27 - third module fully migrated to Postgres.** Reads now serve from PG via `READ_FROM_POSTGRES_PEOPLE=submissions` + `READ_FROM_POSTGRES_DASHBOARD=submissions` (per-module overrides; both handlers consume submissions); dual-write remains ON as the rollback net (`DUAL_WRITE_TABLES` extended with `submissions` alongside news_interactions + 4 directory tables). 109 historical rows backfilled cleanly via `npm run backfill:submissions -- --execute` (106 with admin_action_at preserved via a sidecar raw col-K read; documented as the ONE place the backfill reaches behind the canonical abstraction). **PR D caught a real correctness bug at cutover verification**: per-adapter `new Date().toISOString()` calls in the dual-write orchestrators produced a ~226ms timestamp drift between Sheets col A and PG `created_at`/`submitted_at` on every new submission (verified empirically with a live test row showing `2026-05-27T20:30:11.583Z` Sheets vs `2026-05-27T20:30:11.809+00:00` PG). Fix: orchestrators now stamp the event-moment ONCE before dispatching to both adapters; adapter signatures take an optional 4th param with `|| new Date()` fallback for backward compatibility. Verified live post-deploy: all writes post-20:53:05 UTC byte-equal across stores; one pre-fix row carries the historical 226ms drift (no token-math impact, documented as artifact). **Module 1 + Module 2 + Module 3 cutovers all LIVE on Postgres.** 6 tables / ~189+ rows mirrored. **Shared infrastructure shipped:** `scripts/_lib/backfill-runner.mjs` (263 lines) extracts the reusable engine - header check + optional sidecar reads + validators + three write strategies (upsert / insert-if-empty / replace-null-pool) + post-write count verification. First consumer is `scripts/backfill-submissions.mjs`; `scripts/backfill-directory.mjs` deliberately keeps its embedded runner for now (cleanup PR backlog item to migrate it). **The dual-write + per-module-read pattern is PROVEN LIVE three times** - now generic infrastructure plus the orchestrator-level timestamp coordination requirement. Drop news_interactions + directory + submissions from `DUAL_WRITE_TABLES` later once stability is confirmed - no urgency, rollback net stays on. **NEXT: Module 4 = TBD** between People Portal continuation (incidents structure-only EMPTY per audit, then drafts + notification_log multi-writer) and skipping to next module per migration order. Mode = fast-as-safe (`docs/MIGRATION_APPROACH.md`).
**Stage:** 1 (Supabase Setup + Schema Design) - **3 modules cut over**; Stage 0 ~99%, only Calendar consolidation deferred
**Next milestone:** Module 4 (TBD: People Portal continuation - incidents structure-only EMPTY per audit, drafts + notification_log multi-writer - vs next module per migration order)

---

## How to use this doc

This is the canonical project state for the Sheets → Supabase migration. Update at every session boundary:
- **Session start:** Read this doc, confirm accuracy, proceed.
- **Session end:** Update this doc to reflect work shipped/changed/learned, commit (in the PR if one exists, standalone otherwise).

Visual rendering of this doc is generated via show_widget at session start and end.

This doc supplements `docs/SUPABASE_MIGRATION.md` (the long-form migration plan) - this is the at-a-glance state, that is the detailed reasoning.

---

## Summary metrics

- **PRs shipped:** 49 (since 2026-05-14)
- **Stage 0 progress:** ~99% (only Calendar SA helper consolidation remains, deferred post-Stage-1)
- **Stage 1 progress:** in flight - **3 modules cut over**: news_interactions (2026-05-27 morning) + directory (2026-05-27 afternoon, 4 tables: accounts 13 / contacts 30 / work_locations 12 / hero_images 6) + **submissions (2026-05-27 evening)**. **Project 3 (finance stack migration) scoped 2026-05-28**: audit (PR #83) + plan (PR #84) shipped; 12 PRs across 5 modules (dataStore split / vendor / invoice / Smart Inventory / cron) to bring vendor + invoice + GL_CODES + AI_LINE_ITEMS + inventory + Railway cron all onto Postgres. Module 4 (PR 4.1 dataStore.js split) is the first PR; ships before any finance work. Submissions: 109 historical rows backfilled (106 with admin_action_at preserved via sidecar raw col-K read), `READ_FROM_POSTGRES_PEOPLE=submissions` + `READ_FROM_POSTGRES_DASHBOARD=submissions` live, dual-write on. **PR D drift fix** (per-adapter `new Date()` produced ~226ms timestamp drift between Sheets and PG on dual-writes; orchestrator-level stamp-once eliminates the bug class). 6 PG tables / ~189+ rows mirrored. **scripts/_lib/backfill-runner.mjs** shipped as reusable engine for future per-module backfills. Per-module flag mechanism + shared backfill engine + orchestrator-level timestamp coordination are now generic infrastructure. **Next: Module 4 = TBD** (People Portal continuation - incidents structure-only EMPTY, drafts + notification_log multi-writer - or skip to next module per migration order). **Sheets audit complete** (`docs/SHEETS_AUDIT*.md`); BUILD RULE = migrate from LIVE verdicts, not sheet structure. user_accounts auth mappings drafted. Mode = fast-as-safe (`docs/MIGRATION_APPROACH.md`).
- **Items remaining:** 6 (in 5 bundles + 4 unbundled; CLAUDE.md item 1 closed PR #56; latent directory POST auth gap closed PR #57; directory SA migration complete PR #58; dashboard SA migration + 2-month news_interactions bug fix complete PR #59; auth strategy decision DECIDED + specced via PR #60; only Calendar SA helper consolidation remains as a deferred post-Bundle-3 follow-up)
- **Calendar estimate to Stage 1:** 2-3 months at sustainable pace

---

## Done - 49 PRs shipped to main

| PR | Title | Date | Notes |
|---|---|---|---|
| #23 | Dashboard route dead-read cleanup | 2026-05-14 | -200 LOC, 3x faster load |
| #26 | Broken People reports deleted | 2026-05-14 | -621 LOC |
| #27 | People Portal POST audit | 2026-05-14 | 9/10 clean, 1 deleted, 1 bug fixed |
| #28 | Cron daily audit | 2026-05-14 | Dead home_news block removed |
| #35 | Cron backup-sheets | 2026-05-15 | Documented post-hoc (shipped earlier as PR #14) |
| #40 | People Portal GET audit | 2026-05-15 | 7/8 actions audited, all clean |
| #41 | Audit #1 - Ops Hub dispatcher | 2026-05-17 | BUSINESS_NOTES.md pattern established |
| #42 | Audit #2 - Inventory | 2026-05-17 | 2 bug fixes |
| #43 | opsNotify consolidation | 2026-05-17 | Shadow duplicate eliminated |
| #44 | Audit #3 - Season Tracker | 2026-05-17 | 3 bug fixes, 3 BUSINESS_NOTES entries |
| #45 | EOD handoff doc | 2026-05-17 | 3 PRs + Audits #2+#3 summary |
| #46 | Knowledge file scaffolding | 2026-05-18 | TEAM_KNOWLEDGE.md + SPEC_INTRANET_AI_SEARCH.md |
| #47 | Audit #4+#5 - Invoice + Vendor | 2026-05-18 | 3 bug fixes, 17 knowledge entries, 5 new SA helpers |
| #48 | Bundle 1: Audit #4+#5 follow-up cleanup + project dashboard | 2026-05-18 | triggerAIScan SA, ensureLineItemTab swap (createTabSA), 2 frontend static-components fixes, PROJECT_DASHBOARD.md established |
| #49 | Frontend lint cleanup pass + InvoiceTool bug fix | 2026-05-18 | 12 lint issues closed, 2 set-state-in-effect refactors (Option B derived state), 1 use-before-declare bug fixed, PR #50 scope discovered (InvoiceTool.js still has 13 problems) |
| #50 | Latent stale-closure bug fix in invoice submit handler (+ 2 dead-dep cleanups) | 2026-05-18 | L742 handleConfirmedSubmit was missing ocrResult?.vendorName + resetForm in deps - real bug masked by UX flow, would have caused Supabase data integrity issue. Plus L539 + L600 dead-dep removals. InvoiceTool.js now warning-free; 10 errors remain in PR #51 backlog. |
| #51 | Audit #6 - Smart Inventory (migration-readiness focus) | 2026-05-18 | 30 handlers audited, 2 F-codes fixed (F33 fire-and-forget async forEach in handleMergeItems, F36 dropped reason+email in handleReviewDelete), 12 BUSINESS_NOTES entries + 1 updated, env var inconsistency cleaned (94 sites), new SMART_INVENTORY_DATA_MODEL.md (393 lines) captured for Stage 1 schema design. Stub triage deferred to product session post-migration. |
| #52 | Bundle 2 close-out + knowledge map v1 | 2026-05-19 | Service Calendar deferred (50% built), Railway cron documented (audit-as-documentation, F43-F49 captured not fixed), CLAUDE_KNOWLEDGE_MAP.md v1 added, 2 BUSINESS_NOTES entries (Railway cron invariants + F43-F49 log), dashboard sync carry-forward. Stage 0 progress 82% → 87%. |
| #53 | Bundle 3 sub-phase 0 recon (Sheets access inventory) | 2026-05-19 | 17 Sheets-touching files classified across 6 categories (10 CONSOLIDATED / 1 DOWNSTREAM / 1 DIRECT-ONLY / 1 AUTH-BOUNDARY / 4 AD-HOC-HELPER). New docs/SHEETS_ACCESS_INVENTORY.md (178 lines) becomes Bundle 3 consolidation reference. Recommended 3-PR scope (~10-13 hr total): cron consolidation, directory migration, dashboard auth-boundary decision. Recon-only, no code changes. |
| #54 | Bundle 3 PR A1 - cron consolidation (3 files) | 2026-05-20 | 3 of 4 hand-rolled JWT paths consolidated to canonical getServiceAccountSheetsClient / getServiceAccountDriveClient helpers. New getServiceAccountDriveClient(scopes) helper added to sheets.js (+22 LOC). cron/backup-sheets, cron/daily, cron/incident-reminders all migrated; drift-bomb duplicate SHEET_IDS consts removed. Gmail SA pattern in cron/incident-reminders intentionally preserved (domain-wide delegation, different from Sheets SA). Net -108 LOC. people/route.js (2056 lines) remains for PR A2 to fully close CLAUDE.md item 1. 2 BUSINESS_NOTES entries added (Hand-rolled JWT consolidation [CLOSED ISSUE], Gmail SA auth pattern [PRESERVE]). |
| #55 | Bundle 3 PR A2a - people/route.js Sheets consolidation + Drive client consolidation | 2026-05-22 | Sheets-path portion of CLAUDE.md item 1 closed. 66 call sites in people/route.js migrated to canonical SA helpers (29 readSheet, 4 appendRow, 27 updateCell, 3 updateRow, 1 clearRow, 2 appendRowAnchored). 7 local Sheets helpers removed; ensureIncidentsTab refactored in place to use canonical primitives (D2: keep local). Local SHEET_IDS const dropped, drift-bomb (PEOPLE_DB_SHEET_ID fallback to MASTER_HUB) removed (P0). 2 new canonical helpers added to sheets.js (clearRangeSA, updateCellByRowColSA). Drive client consolidation: 2 duplicate getServiceAccountDriveClient definitions in drive.js + incidentActions.js consolidated to canonical (PR #54 incomplete sweep finished). ops/route.js dead-import cleanup (5 unused imports dropped). Net -143 LOC across 5 code files. 4 BUSINESS_NOTES entries + 6 CLAUDE_KNOWLEDGE_MAP anti-knowledge entries added. Gmail JWT block stays alive for PR A2b (getAccessToken now orphan, removed in A2b). |
| #56 | Bundle 3 PR A2b - Gmail SA canonicalization [CLOSES CLAUDE.md ITEM 1] | 2026-05-22 | CLAUDE.md item 1 fully closed - zero active hand-rolled crypto.subtle JWT remains in the codebase. New canonical sendEmailSA({ sender, displayName, to, subject, html, replyTo }) in src/lib/gmail.js consolidates the two SA-impersonated Gmail implementations: people/route.js (hand-rolled crypto.subtle JWT + raw fetch) + cron/incident-reminders (google.auth.JWT + local MIME builder). 3 encodings byte-equivalence-proven before swap (subject via encodeSubjectSA byte-exact port, HTML body standard padded base64, raw message base64url unpadded - empirically verified across 4 forced-character test inputs). people/route.js: removed full ~72 LOC JWT block (5 functions) + 66-LOC local sendEmail; replaced with 1-line adapter closing over GMAIL_SENDER + GMAIL_SENDER_NAME consts; incidentActions.js by-reference contract preserved (0 lines touched there). cron/incident-reminders: removed local getGmailClient + buildEmailMime + dead googleapis import. Two intentional behavior changes to cron emails (RFC 2047 subject encoding + base64 body) for post-merge verification. Two subject encoders coexist in gmail.js as follow-up cleanup (encodeSubject vs encodeSubjectSA differ on control chars; unify later). Net -68 LOC across 3 code files (gmail.js +95, cron -30, people/route.js -133). 1 new + 2 updated BUSINESS_NOTES entries + 4 new CLAUDE_KNOWLEDGE_MAP anti-knowledge entries + Gmail layer added to client landscape. |
| #57 | Bundle 3 PR B1 - Directory POST interim admin gate [SECURITY FIX] | 2026-05-22 | Closed a latent authorization gap: directory/route.js POST authenticated but did NOT authorize. Admin gating was UI-side-only via the isAdmin flag from bootstrap; a non-admin authenticated user could craft direct API calls to admin-* actions (the only backstop was Drive's editor ACL via the user's OAuth token - implicit, brittle, and would have disappeared when PR B2's SA migration removes the user-OAuth dependency). New server-side gate at the POST handler entry parses DIRECTORY_ADMIN_EMAILS (comma-separated env var, lowercased + trimmed + .filter(Boolean)) and returns 403 if session.user.email is not in the list. Fail-closed: missing env var = no one passes. Interim gate, NOT enshrining the admins-tab fudge (people/route.js requires col A + col C "hr"=TRUE; directory bootstrap checks col A only - inconsistent flag model across modules). Real fix deferred to Stage 1 Supabase role model + admin dashboard (part of the auth strategy decision, the last Stage 0 gate). Net +19 LOC in directory/route.js (9 code + 10 comment). Zero new imports, zero new Sheets/Drive calls, no SA migration in this PR. PR B split into B1 (this, security) + B2 (the SA migration, ~-130 LOC refactor, next). |
| #58 | Bundle 3 PR B2 - directory/route.js full SA migration | 2026-05-22 | directory/route.js now 100% canonical SA (was the last major user-OAuth Sheets/Drive file in the codebase). 13 reads migrated to readSheetSA, 13 writes to canonical helpers (updateRangeSA / updateCellSA / appendRowSA / appendRowsSA / clearRangeSA / deleteRowSA + getSheetIdSA), 2 Drive proxy calls to getServiceAccountDriveClient. Three local read helpers (safeReadSheet, inline safeRead, getSheetTabId) removed. Two user-OAuth client constructors (getSheetsClient, getDriveClient) removed. googleapis import dropped. Three helper functions (writeLinks, upsertWorkLocation, removeWorkLocation) dropped their sheets+token params at definitions + all 6 call sites. POST handler token extraction + check removed; if (!session) auth check stays in both handlers. B1 gate (PR #57) BYTE-IDENTICAL to its ship - independent of the SA migration since it reads process.env.DIRECTORY_ADMIN_EMAILS + session.user.email. D.3a Drive decision proven by empirical test: 12/12 production gmapImg URLs SA-readable (test script written, run, deleted before any code changes; cols H+I have zero Drive URLs so the proxy's whole Drive workload is gmapImg). Write-correctness traps handled: hero_images column-write (urls.map(u=>[u]) extending from A1, not single cell), contacts multi-row delete preserves bottom-up .reverse() ordering, contacts multi-row append uses appendRowsSA (plural). Column widths preserved on all appends. Shape-adaptation handled per-site: the two old read helpers returned different shapes (bare array vs {headers,rows}), the 7 safeReadSheet callers got {rows} destructuring added, the 6 bootstrap callers needed no change. Net -114 LOC (548 -> 434). Post-merge write smoke test (edit account, add account, update contacts, update heroes, deactivate+reactivate, view gmapImg, confirm B1 gate still blocks non-admins). Major user-OAuth Sheets/Drive surface remaining: dashboard/route.js (PR C) + invoiceActions.js user-flow paths (intentional, not consolidating). |
| #59 | Bundle 3 PR C - dashboard/route.js SA migration + wrong-arg-shape bug fix | 2026-05-22 | Two things in one PR. (1) SA migration: dashboard/route.js fully migrated user-OAuth → canonical SA. The LAST general-purpose user-OAuth Sheets write surface in the codebase (only intentional invoiceActions.js user-flow paths remain). 12 user-OAuth calls migrated: 2 readSheet → readSheetSA, 2 appendRow → appendRowSA, 8 updateCell → updateCellSA. Import collapsed 7 names → 4 (readSheetSA, appendRowSA, updateCellSA, SHEET_IDS). Token extraction + if(!token) removed from both handlers; if(!session) stays. Cleanest Scenario B: user identity (email) was ALREADY captured as a data column (news_interactions col B = userEmail) from session.user.email, never derived from the OAuth token - the SA swap preserved identity with zero schema touch. File 401 → 394 LOC (-7). (2) WRONG-ARG-SHAPE BUG FIXED: the 8 updateCell calls passed 5 positional args to a 4-arg helper - the tab name "news_interactions" landed in the range slot, the intended range string "C5" landed in the value slot, the real value ("TRUE"/timestamp) was silently DROPPED. Effect: every cell update wrote the intended range string to A1 of news_interactions instead of writing the value to the target cell. Confirmed empirically during recon: news_interactions!A1 returned "E16" (a stray range string) where the header "postId" should be. Data timestamps showed traffic from 2026-03-21 → ~2 MONTHS in production. User-facing impact: first interaction on a post persisted (append path was shape-correct), every SUBSEQUENT interaction on the same post silently did NOT (toggle saved, toggle ack, mark-all-read on already-touched). Hidden by 3 things stacked: catches return success:false instead of throwing, callers use await Promise.all without inspecting returns, low-frequency feature. FRAMING: canonicalization corrected a pre-existing wrong-arg-shape (NOT "accidentally fixed a bug" - this is exactly what canonicalization eliminates). Documented post-merge steps: (a) manual A1 restoration "E16" → "postId" in Sheets UI (migration writes to C/D/E/F only, won't self-heal A1), (b) write smoke test verifying broken save/ack/mark-all-read paths now persist. |
| #60 | Auth model decided + specced (closes last Stage 0 gate) | 2026-05-22 | docs/AUTH_MODEL.md added as the Stage 1 authorization spec. Two orthogonal concepts (is_admin capability + role identity), 6 roles (Corporate HR / Corporate Finance / Director / Regional / Site Leader / Sous/HM), 3 Stage 1 tables (users, user_accounts, accounts), one rule (is_admin OR role-grants-domain OR account-in-mapped-list), one helper (getVisibleAccounts), one scoping layer (target Supabase row-level security; fallback the helper as query-filter). Retires-after-build: directory B1 interim env-var gate → is_admin in users table; people.js "hr" flag → Corporate HR role check; analytics Kevin-only hardcode → is_admin. Phasing deliberately NOT a platform-wide flip: schema first, corporate domain gating second (replaces hr-flag + analytics hardcode), field-role account scoping third module-by-module (finance-sensitive first, smoke-tested per PR). Auth DATA in its own tables (separate from directory contact records, so a contact edit can never change who can log in); auth MANAGEMENT UI as new tab in existing Directory Admin panel. TODO before Stage 1 schema build: Kevin to fill in user_accounts explicit mappings per Site Leader / Regional / Director - explicit lists, NOT derived from geography or key-prefix logic. Docs-only PR, no code, no env config. The LAST Stage 0 gate item closes as a decision; Stage 1 implements. |
| #61 | Stage 1 PR 1 - dual-write scaffolding + news_interactions upsert | 2026-05-26 | Stage 1 begins. Dual-write infrastructure shipped: src/lib/supabase.js (lazy service-role client, only constructed when a cutover flag routes a call to PG), src/lib/cutover.js (DUAL_WRITE_TABLES + READ_FROM_POSTGRES env-var dispatch, two flags / three states per table), src/lib/dataStore.js (logical data layer; first table's adapters for Sheets + Postgres + dispatch). dashboard/route.js news_interactions rewired through dataStore - 13 cell-addressing call sites collapsed into 5 upsert calls + 1 user-filtered read. The cell-addressing model that produced the #59 wrong-arg-shape bug is retired in favor of upsert semantics (Postgres has no cell coordinates, so addressing by primary key (post_id, user_email) is the right abstraction for both backends). 9 sub-paths verified byte-identical to current Sheets writes with flags off (4 actions × existing-row + 4 actions × no-existing-row + 1 bootstrap read). Supabase project live; 3 tables created (news_interactions, users, user_accounts) per AUTH_MODEL.md + this PR's target. SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY wired in Vercel all envs + .env.local; DUAL_WRITE_TABLES + READ_FROM_POSTGRES empty (flags off = Sheets-only behavior). Postgres path PRESENT but DORMANT - cutover flag flip happens later in Vercel env (manual, by Kevin), NOT in code. Net handler change: -30 LOC in dashboard/route.js (394 → 364), +234 LOC dataStore, +90 LOC cutover, +56 LOC supabase, +1 dep (@supabase/supabase-js). Documented post-merge steps: (a) smoke test with flags off - all news features (mark read, save, ack, mark-all-read) must work identically to today; (b) parked follow-up: 1 moderate-severity npm audit vuln from the supabase-js install, address before Friday; (c) Wednesday cutover sequence: flip DUAL_WRITE_TABLES first, watch for divergence, then flip READ_FROM_POSTGRES, then later drop from DUAL_WRITE_TABLES. |
| #62 | chore: npm audit fix - qs transitive bump | 2026-05-26 | Closes the PR #61 parked npm audit follow-up. Lockfile-only patch bump qs 6.15.1 -> 6.15.2 to clear the moderate-severity GHSA-q8mj-m7cp-5q26 carried in via the supabase-js install (vuln chain was actually transitive through googleapis 171.4.0 -> googleapis-common 8.0.1 -> qs 6.15.1, surfaced by the audit re-run on the supabase-js install). Vuln was effectively unreachable in our usage (googleapis-common builds query strings from typed/controlled inputs, never user-supplied comma-format arrays with null entries) but worth getting npm audit to 0 ahead of the Wednesday cutover so it is not noise in cutover monitoring. The actual diff is 3 lines in package-lock.json (version + resolved URL + integrity hash); no package.json change, no source change, no --force, supabase-js (2.106.2) and googleapis (171.4.0) versions unchanged in both package.json and the lockfile. Note: the dry-run predicted 4 wasm package removals alongside the qs bump; the real run made the smaller change (qs-only). Build clean, lint unchanged from main (this chore touches zero source), npm audit at 0. Isolated from all cutover work: no flags touched, no cutover.js / dataStore.js / supabase.js changes. |
| #63 | chore: news_interactions backfill script (cutover prep) | 2026-05-26 | Cutover step 2 prep: standalone one-time script (scripts/backfill-news-interactions.mjs, +206 LOC) to copy the existing Sheets history into the empty Postgres table so every user keeps their read/save/ack state when the read flip happens tomorrow. Dry-run by default (zero side effects, no Postgres connection at all); --execute does the live upsert + post-write count verify. Idempotent via ON CONFLICT (post_id, user_email) DO UPDATE, so re-running after dual-write has populated some rows reconciles cleanly. Coercion mirrors src/lib/dataStore.js exactly (TRUE/FALSE strings -> JS booleans, empty readAt -> NULL, ISO string passes through as timestamptz, email lowercased + trimmed) so a backfilled row is byte-identical to a row dual-write would produce for the same Sheets row. Safety guards: A1 header check aborts with FATAL if A1 is not "postId" (catches column-shift before any write); malformed rows (missing post_id, missing email, non-ISO read_at) are reported and skipped rather than silently transformed; dry-run never constructs the Supabase client. Recon-before-build confirmed: A1 is "postId" (Kevin's fix in place post-#59), 19 total rows, 6 distinct users, 8 distinct posts, 0 blank rows, 0 missing fields, 0 malformed timestamps, 0 duplicate (post_id, user_email) PK pairs (no winner-rule decision needed). Dry-run output reviewed before merge: 19 rows transformed cleanly, sample shape correct (post_id text, user_email lowercased text, JS booleans not strings, read_at as ISO timestamptz). Touches NO runtime code (cutover.js / dataStore.js / supabase.js / dashboard handler all unchanged since #61), NO cutover flags, NO env files. NOT run live yet - tomorrow's cutover step 2 (Kevin's manual run: node --env-file=.env.local scripts/backfill-news-interactions.mjs --execute), followed by DUAL_WRITE_TABLES flip in Vercel, then READ_FROM_POSTGRES flip. |
| #64 | docs: dashboard sync post-#62 + post-#63 | 2026-05-26 | Carry-forward update to PROJECT_DASHBOARD.md for the two same-day chore PRs that landed without dashboard updates in their own commits. Header rewritten for post-#63 state, PRs-shipped bumped 27 -> 29, section header bumped to 29, two new PR rows added with full context (recon stats, dry-run verification, NOT-run-live framing for #63). Docs-only, no runtime code. Establishes the per-session pattern: chore PRs land first, dashboard sync follows as its own small PR rather than coupling docs to each chore. |
| #65 | docs: Sheets audit three-part reference + BUILD RULE | 2026-05-26 | Three-part Sheets-to-Postgres migration reference shipped as one PR (2,239 LOC total across 3 files, no runtime code). **SHEETS_AUDIT.md** (1,726 lines, code side): every read/write op in src/ mapped to spreadsheet/tab/column/file:line/helper/business action, resolves every dynamic + constant + template tab name, per-tab column touch maps, Part 4 anomalies (read-but-never-written cols, column-count mismatches, drift risk per tab, multi-writer tabs). **SHEETS_AUDIT_DATA_SIDE.md** (331 lines): actual sheet contents read 2026-05-26 from all 4 active spreadsheets (HUB / COLLECTION / INVENTORY / AI_LINE_ITEMS), per-tab fill rates, anomalies, mislabels, padding columns, measured account-key format breakdown (7,581 spaces vs 88 hyphens, all hyphens in deferred modules). **SHEETS_AUDIT_SYNTHESIS.md** (182 lines): code x data cross-reference, per-tab verdicts (LIVE / UNUSED-LIVE / DEAD / ORPHAN / REFERENCE / SCHEMA-ONLY / EXTERNAL-WRITTEN / BUG / MISLABELED), cleanup punch list, risk-ranked migration order. Key cross-reference resolutions (neither side alone could produce): item_catalog cols M+N = EXTERNAL-WRITTEN by inventory AI cron not dead, contacts col G = confirmed live bug (read never re-written on edit), 11 HUB tabs DEFINITIVELY ORPHAN (employee_roster + kiosk_info + content tabs + legacy vendors/gl_codes), account-key format = SPACES canonical, hyphen quarantined in deferred modules. **REAL MIGRATION SCOPE: ~25 LIVE tables of substance, not 82** (19 empty cleanup, 6 schema-only defer, ~11 orphans). **BUILD RULE established for all Stage 1+ schema work:** Supabase schema is built from audit LIVE verdicts, NOT from sheets as-is - dead columns / orphan tabs / empty tabs / reference-only columns (e.g. contacts H/I/J Kiosk Emails / Manager / Region notes) do NOT migrate. Every future schema PR builds clean from the verdicts. Recommended migration order from synthesis (risk-ranked): news_interactions (done, cutover tomorrow) -> directory -> People Portal submissions -> incidents -> drafts/notification_log -> Invoice config (vendor_master/vendor_accounts) -> Smart Inventory (with AI cron as external writer) -> invoice_submissions_26 finance-critical -> defer service-calendar/performance/GL_CODES. Reference artifact for the whole Stage 1 migration. |
| #66 | docs: dashboard update 2026-05-26 (post Stage 1 PR 1 + audit) | 2026-05-26 | Carry-forward dashboard update bringing PROJECT_DASHBOARD.md current with the full 2026-05-26 session arc. Header rewritten for post-#65 state. PRs-shipped 29 -> 31, section header bumped to 29 -> 31. Two new PR rows added (#64 dashboard sync, #65 Sheets audit + BUILD RULE) with full context. Recommended-sequence item 11 rewritten from "Stage 1 begins" (stale) to "Stage 1 IN FLIGHT" with the full migration order from the synthesis, the cutover sequence, the BUILD RULE, and the real migration scope (~25 LIVE tables of substance, not 82). Docs only, in-place update, no restructuring. |
| #67 | docs: migration approach update 2026-05-26 (fast-as-safe mode) | 2026-05-26 | New doc `docs/MIGRATION_APPROACH.md` (109 lines) captures the operating-mode shift from maximum-care / capacity-not-speed to fast-as-safe / foundation-first. The strategic plan in `docs/SUPABASE_MIGRATION.md` stays canonical for the "what"; this doc governs the "how/when/pace". KEEP rails: recon-before-build (one pass), dual-write + reversible cutovers, verify-before-flipping-reads, prove-pattern-before-scaling, build-from-audit-verdicts. DROP gold-plating: re-audits, multi-pass recon, per-table PRs for easy append-only tables, pausing at every micro-step. Sequence: news_interactions cutover proves pattern -> hard modules one-at-a-time (directory, People Portal, invoices) -> BATCH easy append-only tables -> defer service-calendar / performance / GL_CODES / orphans. 6-line cross-reference added at the top of `docs/SUPABASE_MIGRATION.md` pointing to the approach update so future sessions discover the new mode from the canonical strategic plan. Goal: whole intranet on Supabase, clean foundation, as fast as is safe. Friday is a progress checkpoint. Docs only, no runtime code. |
| #69 | feat: directory PR A - dataStore adapters (accounts + hero_images, dormant) | 2026-05-27 | Module 2 (directory) PR A: dormant scaffolding shipped as +740 LOC in `src/lib/dataStore.js`, one file, no other code touched. Three shared primitives (foundation for PR B): `coordinatedWrite` (multi-table sequential runner, log-and-continue on failure), `deleteRecord` (Sheets row delete + optional PG DELETE), `replaceScope` (clear-then-bulk-insert by scope). accounts adapters: `getAccounts()` dispatches by `isReadFromPostgres` (Sheets path joins accounts + dir_links by team_key; PG path SELECTs from the folded accounts table); `upsertAccount(teamKey, partial)` dispatches by `isDualWrite`, uses ONE `batchUpdateRangesSA` per upsert instead of Promise.all of N `updateCellSA` (avoids the Sheets per-user 60-write/min rate limit, aligns with the coordinatedWrite design). hero_images adapters: `getHeroImages()` returns flat global pool (PG reads filter to `team_key IS NULL`); `replaceHeroImages(urls)` clears + rewrites Sheets, replaces NULL-team_key rows in PG (team_key always NULL on write today, dormant per-account infrastructure). Built from `docs/SHEETS_AUDIT_SYNTHESIS.md` LIVE verdicts: accounts cols N/O/P/Q (Wifi/Gate/Door) DROPPED per DEAD verdict; dir_links folded into accounts on PG (4 URL columns); col T (region) EXCLUDED from writeable set (the latent-blanking-risk fix from the directory design - region is populated externally). hero_images team_key NULLABLE; always written NULL today. New-row append A-R parity with current handler (18 cells, no col S; blank col S read as active=TRUE via sheetActiveToBool coercion). batchUpdateRangesSA signature verified against production callers (invoiceActions, inventoryActions, leadership-dugout) before merge. Directory handler NOT rewired - `src/app/api/directory/route.js` byte-identical to post-#58. With cutover flags off (default), nothing in the new section executes. Same zero-merge-risk dormant pattern as Stage 1 PR 1. |
| #70 | docs: dashboard update 2026-05-27 (post-cutover + PR A) | 2026-05-27 | Combined state-of-truth dashboard update bringing PROJECT_DASHBOARD.md current with the actual state on main: news_interactions CUTOVER COMPLETE (from earlier the same day - reads from Postgres, dual-write on as rollback net, 18 rows matching, pattern proven live end-to-end) + directory PR A MERGED (#69, dormant dataStore adapters + 3 shared primitives, batchUpdateRangesSA write mechanism). PRs shipped 31 -> 34, section header bumped, three new PR rows added (#66, #67, #69). Header rewritten. Next milestone set to directory PR B. Recommended-sequence item 11 updated to show PR A as MERGED + PR B as next. PR #68 (orphaned interim dashboard update) was closed earlier in the session with a supersede comment - its content shipped to main via PR #69's combined diff. Docs only, no runtime code. |
| #71 | feat: directory PR B - handler rewire + contacts/work_locations + per-module flags | 2026-05-27 | Module 2 (directory) ACTIVATION PR. Rewires `src/app/api/directory/route.js` to call the dataStore for all 4 directory tables (accounts, contacts, work_locations, hero_images). Adds contacts adapters (`getContacts`, `replaceContactsForAccount` with the col-G fix) and work_locations adapters (`getWorkLocations`, `upsertWorkLocation`, `deleteWorkLocation` using the deleteRecord primitive from PR A). Extends `src/lib/cutover.js` with the **per-module READ flag mechanism**: `isReadFromPostgres` now accepts optional `moduleName` arg, OR-composes with global flag, generic env-var-prefix discovery (any `READ_FROM_POSTGRES_<MODULE>` is picked up at module load with no code change). Rationale: accounts is read by 5 modules; tab-level flag would flip all 5 at once; per-module flag lets directory cut over first as the data owner before other consumers migrate. **Three ADDITIVE FIXES** that emerge from the rewire (not regressions, same class as the news_interactions wrong-arg-shape fix that PR #59 surfaced): (1) **col-G slack_user_id preservation** - pre-PR-B contacts edit silently blanked col G (every save lost the Slack User ID, breaking the Slack deep-link button); now carried forward by email match against existing rows. (2) **cols H/I/J REFERENCE preservation** - Kevin's manual notes (Kiosk Emails / Manager / Region) preserved on edit via the same email-match mechanism. (3) **realKey format consistency** - pre-PR-B's `admin-update-contacts` fell back to safeId-encoded key (hyphens) when the account had zero contacts; now resolves via accounts.find so the raw team_key (spaces) is always used. Today's data is unaffected (all 12 accounts have contacts) but the latent bug is fixed for future zero-contacts edges. Byte-identity verified explicitly with flags off: field-by-field trace of the 24-field bootstrap team object (id/rawKey/name/level/city/state/season/stadium/img/logo/address/lat/long/tz + dead cols N-Q hardcoded ""/gmapImg/active/links/contacts), exact links map keys ("Homestand"/"Service Level Agreement (SLA)"/"Service Calendars"/"Drive") with `_hasDirLinksRow` tracking to preserve the no-row -> `{}` vs row-exists -> 4-keys distinction, sort order (MLB / PDC / MiLB then alpha), character-for-character work_locations locationName formula, semantic trace of all 6 POST actions showing identical final cell values. Net diff: cutover.js +75 / dataStore.js +499 / directory/route.js net +227 (-276 +503). Same zero-merge-risk dormant pattern; flags off = byte-identical to pre-PR-B. Activation under flag control happens after merge as a manual Vercel step (DUAL_WRITE_TABLES adds the 4 directory tables -> run 4-table backfill scripts --execute -> READ_FROM_POSTGRES_DIRECTORY=accounts,contacts,hero_images,work_locations). |
| #72 | docs: dashboard sync post-PR-#71 (directory PR B activation) | 2026-05-27 | Dashboard sync after PR A + PR B both merged. PRs shipped 34 -> 36 (added rows for #70 and #71). Header rewritten for post-#71 state: module 2 fully built dormant, 3 additive fixes called out, per-module flag mechanism noted as generic infrastructure. Next milestone set to 4-table backfill scripts. Stage 1 progress bullet captures both PRs merged + cutover sequence ahead. Recommended-sequence item 11 updated to show PR B as merged + cutover as next action. Docs only. |
| #73 | feat: directory backfill script + reusable path-alias loader | 2026-05-27 | Parameterized backfill for the 4 directory tables (`npm run backfill:directory -- --table=<TABLE> [--execute]`). Same pattern as PR #63's news_interactions backfill: dry-run default, --execute required for live, idempotent (ON CONFLICT upserts for accounts/contacts/work_locations; DELETE WHERE team_key IS NULL + INSERT for hero_images since the partial unique index can't be addressed via Supabase's onConflict). Reuses dataStore.readXSheets so transform logic stays in one place; 4 export keyword additions to dataStore.js (additive public API, no behavior change). **REUSABLE INFRASTRUCTURE shipped alongside**: `scripts/_setup/path-alias-loader.mjs` + `register-aliases.mjs` form a Node loader hook that resolves `@/lib/*` aliases for any future script (mirrors jsconfig.json), invoked via `--import` flag. Explicit error message when alias matches but file is missing (`[path-alias-loader] @/ alias resolved to no existing file. Specifier: ..., Tried: ...`). Convenience npm script `backfill:directory` wraps the full --import invocation. Generic infrastructure - every future backfill script in the easy-append-only batches will use this same hook with zero per-script setup. Hit the documented e2e infrastructure flake (PR #50 precedent) on first CI run; re-run passed in 60s with identical commit, confirming the failure was production-side flake not a regression. Verified: build clean, lint clean, em-dash 0, all 4 tables dry-run successfully against live Sheets data with expected counts (12 / 30 / 12 / 6), error-throwing branch tested with intentional missing alias. |
| #74 | docs: dashboard sync - Module 2 (directory) CUTOVER COMPLETE | 2026-05-27 | Carry-forward dashboard update after directory cutover completed end-to-end. PRs shipped 36 -> 37, section header bumped. Header rewritten to capture the cutover sequence: `DUAL_WRITE_TABLES` extended -> backfills ran one-at-a-time (60 rows total) -> `READ_FROM_POSTGRES_DIRECTORY=accounts,contacts,hero_images,work_locations` flipped -> per-module flag mechanism worked end-to-end (directory reads on PG, other modules reading shared tabs unaffected). Three additive fixes from PR B (col-G slack_user_id preservation, cols H/I/J REFERENCE preservation, realKey format consistency) called out as now LIVE in production. Recommended-sequence item 11 updated to show Module 2 cutover complete + Module 3 next. Docs only, no runtime code. |
| #75 | feat: submissions PR A - dataStore adapters + token translation (dormant) | 2026-05-27 | Module 3 (People Portal submissions) PR A: dormant scaffolding in src/lib/dataStore.js for the submissions tab. Sheets + Postgres adapters (read + upsert + update-status), sub-{N} token translation (virtual index over `ORDER BY created_at ASC`; sheet path = sheet row N, PG path = OFFSET N-2 LIMIT 1), canonical record shape exposing both `createdAt` (immutable origin) and `submittedAt` (mutable last-touched) via the dual-column timestamp design refined mid-build. Schema ALTER applied to PG submissions table: `submitted_at TIMESTAMPTZ NOT NULL DEFAULT now()` added alongside `created_at TIMESTAMPTZ NOT NULL` (no default - must be set explicitly on INSERT, matches immutable-origin intent). User-facing date display reads `submittedAt` which matches today's Sheets col A behavior byte-identically at cutover; no user-facing divergence. STATUS DEFAULT GOTCHA documented (column DEFAULT 'Pending' only fires on column-omitted INSERTs; the adapters OMIT the status key when input is falsy/empty). admin_action_at intentionally NOT exposed in the canonical read shape (write-only audit trail per design). dashboard/route.js's `safePeopleFetch` rewired through `getSubmissions({ module: "dashboard" })` so the dashboard cutover can sequence INDEPENDENTLY of the people module via `READ_FROM_POSTGRES_DASHBOARD`. people/route.js NOT rewired (PR B). With flags off the dataStore's Sheets adapter does the same readSheetSA call the handler used to do directly, returning canonical shape. Smoke test verified byte-equal at PR-merge time across all 109 rows. |
| #76 | feat: submissions PR B - handler rewire (dormant) | 2026-05-27 | Rewires src/app/api/people/route.js to route ALL submissions reads + writes through the dataStore. 8 sites rewired: bootstrap counts, history, admin-queue, admin-queue-closed, submit-newhire POST, submit-paf POST, withdraw/cancel-submission, admin-process. Read sites use `getSubmissions({ module: "people" })`; write sites use `upsertSubmission` (with `try/catch` preserving the `{ success: false, error }` return shape) and `updateSubmissionStatus`. Pre-existing handler quirks preserved faithfully per byte-identity contract (no scope creep): module defaults to "paf" at call sites, location defaults to "Unknown", payload JSON guard at call sites, status `.trim().toLowerCase()` preserved, Site 6 location ternary precedence quirk preserved as-is, `rowIndex` field in history response preserved as numeric (derived from `parseInt(sub.token.slice("sub-".length), 10)`), `{ success: false, error }` return shape preserved. Site 8 admin-process notification re-read changes from full-table read + rowIndex lookup to `getSubmissionByToken(token)` - incidental perf win on PG path post-cutover. Pre-rewire empirical checks vs live submissions sheet (109 rows): blank module 0/109, blank location 0/109, non-JSON payload 0/109 - all handler defaults are dead code paths in real data but preserved anyway for defense-in-depth byte-identity. Comprehensive read-site smoke test (OLD positional iteration vs NEW canonical iteration, all 4 read sites): Site 1 bootstrap counts MATCH for k.fietek (paf=1, newHire=0, actionRequired=0, completedTotal=25), Site 2 history all 26 items byte-equal, Site 3 admin-queue all 3 pending items byte-equal, Site 4 admin-queue-closed all 106 closed items byte-equal. Five pre-existing `no-assign-module-variable` lint errors preserved (LHS rule fires on the `const module = ...` pattern; PR changed the RHS, kept the LHS - 0 new lint sites introduced). |
| #77 | feat: submissions PR C - parameterized backfill (109 rows) + shared runner | 2026-05-27 | Two new scripts + 1 npm script. **scripts/_lib/backfill-runner.mjs** (263 lines): reusable engine extracted as shared infrastructure for future per-module backfills. Config-driven orchestration: header check, optional sidecar read with index alignment, validators (skip-with-log), transform, sample preview, dry-run gating, supabase client setup, three write strategies (upsert / insert-if-empty / replace-null-pool), post-write count verification. **scripts/backfill-submissions.mjs** (154 lines): first consumer of the shared runner. Strategy = `insert-if-empty` (pre-flight count check aborts if PG table is non-empty, preventing accidental clobbering of dual-written rows; chosen because submissions has no natural UNIQUE column suitable for upsert and adding one was rejected as a schema change out of scope). **Col K (admin_action_at) preservation via sidecar read**: canonical layer intentionally does NOT expose col K, but the BACKFILL wants to preserve historical audit-trail values; script does ONE raw readSheetSA alongside the canonical read, indexes by position, merges col K into the transform payload. **Defensive parse on col K**: `Date.parse` + round-trip through ISO so valid timestamps land cleanly and invalid ones skip-with-log (submission row still inserted, just without admin_action_at). Live execute against PG: 109 rows inserted, 106 with admin_action_at preserved (audit estimate was ~104, slightly higher), 0 parse warnings, status counts match PR B smoke test (3 pending / 103 complete / 3 other terminal). Token math verification: sub-2 -> first row by created_at ASC (k.fietek 2026-03-23), sub-110 -> last row (j.poletti 2026-05-26). Timestamp format note empirically verified safe: PG TIMESTAMPTZ returns `+00:00` suffix vs Sheets `Z`; grep across src/ for `sub.createdAt`/`sub.submittedAt` found 3 hits all in PR B's rewired sites, all wrapped in `new Date(...).toISOString()` roundtrip - 0 (b)-class risks. `scripts/backfill-directory.mjs` intentionally UNCHANGED; cleanup PR backlogged to migrate it to the shared runner. New npm script `backfill:submissions`. |
| #78 | fix(submissions): stamp timestamps once at orchestrator level (drift bug) | 2026-05-27 | Real correctness bug caught during PR C cutover verification. **The bug**: each dual-write orchestrator (`upsertSubmission`, `updateSubmissionStatus`) dispatched to two adapters sequentially (Sheets first, then Postgres), and each adapter independently called `new Date().toISOString()` to generate the event timestamp. The wall-clock between the two sequential adapter calls became a real drift between the two stores for the SAME logical event. Verified empirically with a live new-hire submission: Sheets col A = `2026-05-27T20:30:11.583Z`, PG `created_at` = `2026-05-27T20:30:11.809+00:00` (226ms drift). **Three implications (normal traffic, not pathological)**: (1) token math fragile for concurrent calls - two near-simultaneous submissions that interleave their adapter calls could yield different orderings (Sheets A,B but PG B,A); (2) audit/forensic noise - same event has two timestamps; (3) clean line at fix-deploy moment - all writes after merge match across stores. **Surface area** (mapped read-only before patching): only the two submissions orchestrators are affected; other modules' per-adapter `new Date()` calls are PG-only `updated_at` writes with no parallel Sheets timestamp to drift against. **The patch**: orchestrators stamp the event-moment ONCE before dispatching, pass it explicitly to both adapters; adapters take an optional 4th param with `|| new Date()` fallback for backward compatibility. `upsertSubmission` stamps `createdAt`; `updateSubmissionStatus` stamps `adminActionAt` (PG `submitted_at` and `updated_at` stay on local fresh now since they're PG-only metadata with no parallel Sheets timestamp). **Live byte-equal verification post-deploy**: row submitted + admin-processed after the deploy went green (20:53:05 UTC) shows MATCH on both `created_at` and `admin_action_at` across stores. One historical row (the pre-fix submission that exposed the bug) carries the 226ms drift permanently - no token-math impact (sub-second drift doesn't reorder against neighbors hours apart), documented as artifact, not patched. **BLOCKED `READ_FROM_POSTGRES_PEOPLE` flip until merged + verified**; flip happened post-PR-D verification. **Lesson captured**: dual-write timestamp drift is its own correctness-bug class; orchestrators must coordinate event-moments before dispatching, never rely on per-adapter `new Date()`. |
| #79 | docs: end-of-session sync - Module 3 (submissions) cutover complete | 2026-05-27 | Carry-forward update after Module 3 cutover. PRs shipped 38 -> 43, section header bumped. Header rewritten for Module 3 cutover complete with PR D drift fix narrative. 5 new PR rows added (#74-#78). Recommended-sequence item 11 rewritten for 3 modules cut over with generic infrastructure inventory (per-module flag, path-alias loader, shared backfill runner, orchestrator-level timestamp coordination). docs/journal/journal.txt created. Docs only. |
| #80 | fix(hero_images): preserve A1 header row in replaceHeroImagesSheets | 2026-05-28 | Latent bug surfaced by overnight investigation. Canonical reader `readHeroImagesSheets()` calls `readSheetSA()` which treats row 1 as headers, but hero_images had no header row - so row 1 (a real URL) was silently dropped. Pre-fix `replaceHeroImagesSheets` cleared `A:A` and wrote URLs from `A1`, self-perpetuating the bug. Fix: clear `A2:A` only, write URLs from `A2`, leave A1 untouched. Defense-in-depth: `expectedFirstHeader: "ImageURL"` added to backfill-directory.mjs hero_images config. Verified end-to-end: Sheets headers = "ImageURL", canonical reader returns 5 URLs matching PG 5 URLs (byte-equal sets). |
| #81 | refactor(backfill): migrate directory script to use shared runner | 2026-05-28 | Second consumer of `scripts/_lib/backfill-runner.mjs` extracted in PR #77. Reduces `scripts/backfill-directory.mjs` from 350 to 221 lines (-129) by deleting the embedded orchestration runner. Per-table strategies + transforms + conflict rules + CLI args preserved. hero_images dedupe preserved via readSheets wrapper. Tightly bounded runner fix: dry-run hint produced a double `--` when per-module npmCommand already contained `--` (directory case). 3-line conditional detects existing `--` in npmCommand and skips the duplicate. Verified clean across all 4 directory tables AND submissions (regression check). Net: 2 files, +86/-211 (-125 lines). |
| #82 | docs: align cutover.js doc block with actual code behavior | 2026-05-28 | Discovered during Project 2 pre-flight (decommission news_interactions). The top doc block in src/lib/cutover.js made two FALSE claims: (1) "removing from DUAL_WRITE_TABLES makes Sheets writes stop" - WRONG (orchestrators write Sheets unconditionally); (2) "isReadFromPostgres asserts isDualWrite at call time" - WRONG (pure boolean lookup, no assertion). Combined: the documented "final decommission" step would create an illegal state with no runtime guard. Rewrite: top doc block accurately describes the 4 states (3 supported + 1 misconfiguration), adds ORCHESTRATOR PATTERN section, adds DECOMMISSION NOTE (KNOWN LIMITATION), captures Sheets API availability dependency, replaces speculative example with actual 3-module historical cutover sequence. Comments-only change confirmed via diff filter. Caught a real architectural concern BEFORE the env change Kevin was about to make. |
| #83 | docs: finance stack code audit (substrate for Project 3 planning) | 2026-05-28 | Comprehensive code audit of the finance stack (Vendor Portal + Invoice Capture + Smart Inventory + AI_LINE_ITEMS + Railway cron + shared infrastructure) in preparation for Project 3. 1266 -> 1294 lines after review revisions across 10 sections: overview + top-line findings, per-file audit (26 files: shared infra, vendor, invoice, inventory, cron), cross-cutting findings, drift report (3 HIGH + 2 LATENT + 6 MEDIUM + 7 LOW), undocumented behaviors (18 items), schema reality check (16 tables with PG design sketches incl. zone_corrections schema captured live), multi-writer map (5 shared tables with 2-11 writers each), critical dependencies (Bill.com / Rippling / AP / Slack / Drive / Anthropic), open questions (8 -> after Kevin's review folded into plan), recommended planning approach. Key finding: opsUtils.getAllVendors + resolveVendorId have positional-index drift BUT zero live callers (LATENT not LIVE). zone_corrections tab exists with 9-col schema; never used. handleVendorMerge missing admin gate. 5-7 vendor matchers + 5 CATEGORIES declarations identified for consolidation. Section 2 reordered 2A -> 2E from initial reverse-stacked state during review. |
| #84 | docs: Project 3 finance stack migration plan | 2026-05-28 | Working reference for Project 3. Every PR in Project 3 traces back to this artifact. 1087 lines across 9 sections: scope statement, target architecture (full DDL for 16 PG tables incl. zone_corrections, vendor_aliases normalized table, invoice_rejections separate table, GENERATED columns, partial UNIQUE INDEX for F24 dedup, ON DELETE SET NULL on review_queue FK), decision log (17 locked decisions), migration sequence (5 modules / 12 PRs / 105-145 hours with parallelism rules saving 5-7 days calendar time), cutover protocol with per-module Vercel env progression and cron-coordination rules, risk register (15 risks), what stays unchanged (8 invariants from THREESYSTEMS brief), open items (all 6 resolved during review with Kevin's locked answers - service-role-only RLS, primary_vendor log+null, manual pre-backfill dedup cleanup, sequential cutover with schema PR parallel, 7 stub handlers confirmed, is_purchasing existing exclusions), doc maintenance rules. Final-review refinements: PR 5.3 + PR 5.4 bundled (3-5 hrs, saves one review cycle), parallelism rules subsection captures aggressive cross-module overlap strategy. PR count 13 -> 12. Plan is execution-ready. |

---

## Bundle 1 - Invoice + Vendor follow-up cleanup

**Status:** SHIPPED 2026-05-18 as PR #48
**PR slot:** #48 candidate
**Effort:** ~2-3 hours

Items:
- [x] Create docs/PROJECT_DASHBOARD.md (sub-phase 1) - 165 lines, canonical project state established
- [x] triggerAIScan + updateScanStatus user-OAuth swap (sub-phase 2) - 2 appendRowsSA swaps; updateScanStatus surfaced as no-op
- [x] ensureLineItemTab raw Sheets v4 fetch swap (sub-phase 3) - createTabSA helper added; 3 raw fetches → 3 helper calls; codebase now 100% free of raw sheets.googleapis.com fetches in invoiceActions.js
- [x] Frontend React static-components fixes - 2 of 3 files (sub-phase 4 carve-out) - SkeletonLoader (InvoiceTool.js) + StepBar (VendorSetup.js) lifted to module level

Rationale: Same mental model as PR #47. F17 pattern continuation. Context fresh from sub-phase 5 of PR #47.

---

## Bundle 2 - Audit close-out

**Status:** QUEUED
**PR slots:** #49, #50, #51
**Effort:** 1-2 days

Items:
- [ ] Audit #6 - Smart Inventory (`/api/ops/inventory` subroute, 117 lines) - T1, ~1-2 hr
- [ ] Service Calendar route audit - T1, half-full day
- [ ] Cron routes audit - Railway side + verify Vercel.json coverage - T1, 1 session

Rationale: Same audit-style methodology, different routes. Keep as separate PRs for clean review surfaces. Schedule close together while audit muscle is sharp.

---

## Bundle 3 - Data layer foundation

**Status:** QUEUED
**PR slot:** #54 candidate
**Effort:** 3-4 sessions

Items:
- [ ] Refactor src/lib/sheets.js into clean data-access layer (T2)
- [ ] Document the abstraction interface (T2)

Rationale: Code + docs ship together. 5 helpers from PR #47 form 70% of this. Stable interface so Stage 2 can swap Sheets implementation without callers caring.

---

## Bundle 4 - Knowledge synthesis

**Status:** QUEUED (after Bundles 2+3)
**PR slots:** #55-#57 (docs-only)
**Effort:** 5-8 sessions

Items:
- [ ] Cross-reference all reads with actual sheet contents (T1)
- [ ] Create docs/DATA_MODEL.md - source of truth for Stage 1 schema (T1)
- [ ] Audit findings rollup document (T2)
- [ ] Image hosting catalog for Stage 2a.5 prep (T2)

Rationale: Synthesize accumulated audit knowledge into Stage 1-ready form. Requires audits done first so target stops moving.

---

## Bundle 5 - Operational confidence baseline

**Status:** QUEUED (can parallel Bundle 3)
**PR slots:** #58-#59
**Effort:** 3-4 sessions

Items:
- [ ] Backup strategy testing - verify Railway cron + Drive backup-sheets end-to-end (T3)
- [ ] Performance baseline measurement - top 10 pages/endpoints (T3)
- [ ] Disaster recovery runbook - first draft (T3)
- [ ] Existing access patterns documented (T4)

Rationale: Measure before changing. Becomes regression test bar for Stage 2 cutover.

---

## Unbundled items - scheduled standalone

| Item | Tier | Effort | Notes |
|---|---|---|---|
| Auth strategy decision (NextAuth vs Supabase Auth) | T1 | 2-3 sessions | Danger zone. FINAL pre-Stage-1 gate. |
| F12 full fix - chef-request vendor deactivation workflow | T3 | 1-2 days | Feature work, can ship anytime |
| Slack notification consolidation audit | T3 | 1 session | Different code surface from route audits |
| Postgres familiarity warmup | T4 | 1-3 sessions | Hands-on practice, not a PR |
| Read 2-3 Supabase migration writeups | T4 | few hours | Reading time |
| Slack with Joe + Britt about migration wishlist | T4 | 1 conversation | Async |
| Test coverage decision | T4 | 30 min | Pure thinking session |

---

## Recommended sequence

1. ~~Bundle 1~~ - ✅ SHIPPED 2026-05-18 as PR #48
2. ~~PR #49~~ - ✅ SHIPPED 2026-05-18 - closed deferred items + 1 bug fix; verified live working
3. ~~PR #50~~ - ✅ SHIPPED 2026-05-18 - latent stale-closure bug fix in invoice submit handler + 2 dead-dep cleanups
4. ~~Bundle 2 - Audit #6: Smart Inventory~~ - ✅ SHIPPED 2026-05-18 as PR #51 - 2 F-codes fixed, 12 BUSINESS_NOTES + SMART_INVENTORY_DATA_MODEL.md captured for Stage 1
5. ~~Bundle 2 - Service Calendar audit~~ - DEFERRED 2026-05-19 - tour revealed ~50% complete development state, not production, not ready for migration-readiness audit. Revisit when Kevin signals Service Calendar is closer to stable.
6. ~~Bundle 2 - Railway cron audit~~ - DOCUMENTED 2026-05-19 - production cron at kitchfix-inventory-cron repo is stable (408 invoices processed, zero observable data loss). F43-F49 captured but not fixed; no production impact evidence. Stage 1 invariants documented in BUSINESS_NOTES.
7. ~~Bundle 3 sub-phase 0 (recon)~~ - ✅ SHIPPED 2026-05-19 as PR #53. ~~Bundle 3 PR A1 (cron consolidation, 3 files)~~ - ✅ SHIPPED 2026-05-20 as PR #54. ~~Bundle 3 PR A2a (people/route.js Sheets consolidation + Drive consolidation + ops dead-imports)~~ - ✅ SHIPPED 2026-05-22 as PR #55. ~~Bundle 3 PR A2b (Gmail SA canonicalization + crypto.subtle JWT block removal)~~ - ✅ SHIPPED 2026-05-22 as PR #56. **The Bundle 3 hand-rolled-JWT-consolidation sub-bundle (PR A1 + A2a + A2b) is COMPLETE - CLAUDE.md item 1 CLOSED.** PR B SPLIT into B1 (security fix) + B2 (the SA migration). ~~Bundle 3 PR B1 (directory POST interim admin gate)~~ - ✅ SHIPPED 2026-05-22 as PR #57. ~~Bundle 3 PR B2 (directory/route.js full SA migration)~~ - ✅ SHIPPED 2026-05-22 as PR #58. **The Bundle 3 directory sub-bundle (B1 + B2) is COMPLETE - directory is 100% SA + has the interim admin gate.** ~~Bundle 3 PR C (dashboard/route.js SA migration + wrong-arg-shape bug fix - -7 LOC + ~2 months of news_interactions data-loss bug fixed as a direct consequence of canonicalization)~~ - ✅ SHIPPED 2026-05-22 as PR #59. **The Bundle 3 SA migration sub-bundle is COMPLETE - all general-purpose user-OAuth Sheets writes canonicalized. The last general-purpose user-OAuth Sheets write surface is closed.** Only invoiceActions.js user-flow paths remain on user-OAuth (intentional, NOT a consolidation target). **Stage 0 close-out:** ~~Stage 1 auth strategy decision (the LAST Stage 0 gate)~~ - ✅ DECIDED + SPECCED 2026-05-22 as PR #60 (`docs/AUTH_MODEL.md`). Pending Stage 1 build (schema + Kevin's user_accounts mapping data entry). After build, retires directory B1 interim env-var gate + people.js "hr" flag + analytics Kevin-only hardcode. **Calendar SA helper consolidation** (3 inline constructions in incidentActions.js + wowPlanActions.js) is the only remaining Bundle 3 follow-up item, deliberately deferred to post-Stage-1 since Calendar isn't migrating to Postgres anyway. **Stage 0 is effectively complete on the decisions axis - Stage 1 work (Supabase project, schema, users + user_accounts tables, phased role enforcement) is now unblocked.**
8. **Bundle 5** in parallel with Bundle 3
9. **Bundle 4** - Knowledge synthesis (after audits + abstraction settle)
10. **Unbundled items** spread throughout - auth strategy decision is the LAST gate
11. **Stage 1 IN FLIGHT - 3 modules cut over.** Supabase project live, schema design via the `docs/SHEETS_AUDIT*.md` three-part reference, phased module-by-module cutover. **Module 1 (news_interactions) CUT OVER 2026-05-27** - PR #61 + #63; cutover executed (18 rows loaded, all interaction types verified persisting through PG read path). **Module 2 (directory) CUT OVER 2026-05-27** - PR #69 (PR A dormant adapters + 3 shared primitives) + PR #71 (PR B handler rewire + per-module READ flag mechanism) + PR #73 (parameterized 4-table backfill + reusable path-alias loader); cutover executed (60 rows backfilled, `READ_FROM_POSTGRES_DIRECTORY` flipped, 3 additive fixes from PR B live in production). **Module 3 (People Portal submissions) CUT OVER 2026-05-27** - PR #75 (PR A dataStore adapters + token translation + dual-column timestamp design `created_at` immutable + `submitted_at` mutable + dashboard cross-module read wired) + PR #76 (PR B handler rewire across 8 sites, byte-equal verified across all 4 read sites and 109 rows for k.fietek) + PR #77 (PR C parameterized backfill 109 rows + shared `backfill-runner.mjs` extract + col K `admin_action_at` preservation via sidecar read with defensive `Date.parse`) + PR #78 (PR D drift fix - per-adapter `new Date()` produced ~226ms timestamp drift, fixed by stamping event-moment ONCE at orchestrator level before dispatching). Cutover executed: `DUAL_WRITE_TABLES` extended with `submissions` -> backfill ran clean (109 rows loaded, 106 with admin_action_at preserved, 0 parse warnings) -> drift bug caught at live verification, PR D shipped + deployed (20:53:05 UTC) -> live byte-equal re-verified (post-deploy writes MATCH on both `created_at` and `admin_action_at` across stores) -> `READ_FROM_POSTGRES_PEOPLE=submissions` + `READ_FROM_POSTGRES_DASHBOARD=submissions` flipped in Vercel -> verified visually (28 history items + 3 admin queue items for k.fietek). One historical row carries a permanent 226ms drift artifact (pre-PR-D submission), no token-math impact. **The dual-write + per-module-read pattern is PROVEN LIVE for the 3rd time** - plus the orchestrator-level timestamp coordination requirement is now generic infrastructure. **Migration order (from synthesis):** news_interactions (CUT OVER) -> directory (CUT OVER) -> People Portal submissions (CUT OVER) -> People Portal incidents (structure-only EMPTY per audit) -> People Portal drafts + notification_log (multi-writer coordination) -> Invoice config -> Smart Inventory (with AI cron as external writer) -> invoice_submissions_26 finance-critical -> defer service-calendar/performance/GL_CODES. **BUILD RULE** (PR #65): Supabase schema follows audit LIVE verdicts, NOT sheet structure. **Real migration scope: ~25 LIVE tables of substance, not 82.** **Generic infrastructure shipped over the 3 cutovers**: per-module flag mechanism (no code change to add new modules, just set `READ_FROM_POSTGRES_<MODULE>` env var); path-alias loader (`scripts/_setup/`); shared backfill runner (`scripts/_lib/backfill-runner.mjs` with upsert/insert-if-empty/replace-null-pool strategies + sidecar reads + validators); orchestrator-level timestamp coordination requirement for dual-writes. user_accounts auth mappings drafted (4 admins + Regionals: Shane=East / Ryan=West / Dec=both + 21 site staff); account-key format MEASURED canonical = spaces ("CIN - OH"); hyphen quarantined in deferred modules. **NEXT: Module 4 = TBD** (People Portal continuation - incidents structure-only EMPTY, then drafts + notification_log multi-writer - vs skip to next module per migration order). **Open backlog items**: ~~(a) migrate `scripts/backfill-directory.mjs` to use the new shared runner~~ - ✅ SHIPPED 2026-05-27 as PR #81; (b) drop news_interactions + directory + submissions from `DUAL_WRITE_TABLES` eventually (no urgency, rollback net stays on for weeks; pre-flight 2026-05-27 confirmed current cutover.js does NOT support this state - removing from DUAL_WRITE_TABLES stops PG writes not Sheets writes, producing the documented state-4 misconfiguration; PR #82 corrected the cutover.js docs to match reality, flagged as future-work code change); (c) e2e infrastructure fix (P2, overdue); (d) sub-{N} token deprecation in favor of UUIDs (future PR after submissions stable). Mode = fast-as-safe (`docs/MIGRATION_APPROACH.md`). **Project 3 (finance stack) SCOPED 2026-05-27** - audit + plan complete via PR #83 (`docs/FINANCE_STACK_AUDIT.md`, 1294 lines, 26 files / ~16,400 LOC across vendor + invoice + Smart Inventory + cron + shared infrastructure) + PR #84 (`docs/FINANCE_STACK_PLAN.md`, 1087 lines, 16 PG tables with full DDL, 12 PRs / 105-145 hours across 5 modules + the kitchfix-inventory-cron cross-repo). Plus 3 small support PRs same-day: PR #80 (hero_images A1 header preservation - canonical reader was silently dropping row 1, fix self-perpetuating), PR #81 (backfill-directory.mjs migration to shared runner), PR #82 (cutover.js doc accuracy - caught architectural bug while preparing news_interactions decommission). **Execution starts at PR 4.1 (dataStore.js split into module facades) per the plan's sequential cutover + schema-in-parallel parallelism rules. All 17 decisions locked + Section 8 items resolved (RLS service-role-only, primary_vendor log+null, BR4 manual pre-backfill cleanup, sequential cutover, stub list confirmed, is_purchasing exclusions confirmed).**

---

## Investigation TODOs (resolved)

| Item | Status | Resolution |
|---|---|---|
| Cut+Dry invoice number scientific-notation spot-check | RESOLVED 2026-05-18 | COLLECTION sheet stores as string. xlsx export artifact only. F32 [PRESERVE] unchanged. |

---

## Discovered during work - deferred follow-ups

These items surfaced during Bundle 1 execution but were deferred to keep PR scope clean. Each is queued for a future small PR.

**PR #49 - SHIPPED 2026-05-18: Frontend lint cleanup pass + InvoiceTool use-before-declare fix:**
- [x] VendorAddModal.js L37 set-state-in-effect (resolved via derived-state pattern)
- [x] VendorSetup.js L127 set-state-in-effect (resolved via `confirmedForName` name-aware derived state)
- [x] VendorSetup.js L374/L384/L397 no-unescaped-entities (5 mechanical escapes)
- [x] VendorSetup.js L799 react-hooks/purity (resolved as bonus from sub-phase 3 refactor)
- [x] InvoiceTool.js L1289 (now L1294) no-img-element + L84 no-img-element (eslint-disable with runtime-dimensions justification)
- [x] BONUS: InvoiceTool.js `resetForm` use-before-declare bug (real compilation-blocking error, fixed by moving declaration up)

**Actual effort:** ~3 hours (scope grew when InvoiceTool.js was discovered to have 18 problems, not 1)
**Actual lint result:** 25 → 13 problems remaining (all in InvoiceTool.js, deferred to PR #50)

**PR #50 - SHIPPED 2026-05-18: Latent stale-closure bug fix + dead-dep cleanups (narrowed scope):**
- [x] L539 `processPDFFile` - removed dead `invoiceNumber` dep
- [x] L600 `tryOCRScan` - removed dead `invoiceNumber` + `vendor` deps + added protective comment documenting the anti-stale-closure ref pattern
- [x] L742 `handleConfirmedSubmit` - added missing `ocrResult?.vendorName` + `resetForm` deps. **REAL LATENT BUG:** stale-closure that would have caused Supabase data integrity issue if UX changed. The lint rule did exactly what it exists for.

**Outcome:** InvoiceTool.js dropped from 13 lint problems to 10 (all 3 warnings closed, 10 errors remain in PR #51 backlog). PR #50's narrowed scope was the right call - the highest-leverage 3 items shipped while the 10 lower-leverage items remain backlogged.

**PR #51 candidate (deferred from PR #50 narrowing) - InvoiceTool.js React pattern cleanup:**
- 6 `set-state-in-effect` errors (L263, L316, L412, L753, L789, L811) - will likely be absorbed into Bundle 3 data-layer refactor
- 4 `react-hooks/immutability` errors at L804-816 (`calc` function impure-during-render)
- Estimated 2-3 hours if pursued as standalone; could be 0 hours if naturally absorbed into Bundle 3
- Status: BACKLOG. Reassess after Bundle 3.

**Smart Inventory stub triage deferred (PR #51 sub-phase 4):**
- Sub-phase 4 stub triage (7 dead handlers) deferred from PR #51. Decision is product-scope (which features get built post-migration) not audit-scope. Revisit when Smart Inventory enters active development phase post-Supabase migration. Stubs and shells preserved as-is in PR #51.

**E2E CI infrastructure fix (originally flagged 2026-05-15, P2 - cost accumulating):**
- `.github/workflows/e2e.yml` line 44 hardcodes `PLAYWRIGHT_BASE_URL` against production, not PR preview URLs
- Impact: PR-side regressions can't be caught by CI (PR's code never runs in test). Any prod flake blocks unrelated PRs.
- Affected so far: **PR #50** (vendor e2e tests failed despite PR #50 not touching vendor code; re-run on identical commit passed in 75s); **PR #73** (auth setup + dashboard + vendor card-detail timed out on production; re-run on identical commit passed in 60s).
- Pattern: e2e runs against production at `https://kitchfix-intranet.vercel.app`, so any prod hiccup blocks unrelated PRs. Workaround is "re-run the failed job" - costs real engineering time every time it fires, and is firing often enough now to justify scheduling rather than perpetually deferring.
- Proper fix: point `PLAYWRIGHT_BASE_URL` at the PR's preview deployment URL (Vercel exposes via GitHub Actions context). 3 parts including cookie-domain change in auth danger zone. Batches with SA auth consolidation work.
- Status: **SCHEDULE rather than defer.** The "1-2 sessions when batched" estimate has been outweighed by the cumulative re-run cost. Pick it up when the next People Portal pause comes naturally.

**Other discovered items:**
- `updateScanStatus` and `ensureLineItemTab` helpers still accept unused `token` parameter (kept for signature consistency in Bundle 1; can drop in a future "drop dead token params" cleanup pass). Low priority, ~15 min.

These don't block Stage 1 but are good citizens.

---

## Notes for future sessions

- Memory rule #9 enforces dashboard protocol (render at start, update at end)
- Dashboard supplements SUPABASE_MIGRATION.md (long-form) with at-a-glance state
- Update timestamp at top of this file every session
