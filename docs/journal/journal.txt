2026-05-27 - Module 3 (People Portal submissions) cutover. Shipped 4 PRs in one session: #75 (PR A dormant dataStore adapters + sub-{N} token translation + dual-column timestamp model `created_at` immutable / `submitted_at` mutable, with the dashboard cross-module read pre-wired so the dashboard cutover could sequence independently of the people module via `READ_FROM_POSTGRES_DASHBOARD`); #76 (PR B handler rewire across 8 sites in people/route.js - bootstrap counts, history, admin-queue, admin-queue-closed, submit-newhire, submit-paf, withdraw/cancel-submission, admin-process - all preserving pre-existing handler quirks faithfully per byte-identity contract; verified all 4 read sites byte-equal across 109 rows for k.fietek); #77 (PR C parameterized backfill of 109 historical rows including 106 with `admin_action_at` preserved via a sidecar raw col-K read with defensive `Date.parse` round-trip; extracted `scripts/_lib/backfill-runner.mjs` as the reusable engine for future per-module backfills with upsert / insert-if-empty / replace-null-pool strategies + sidecar reads + validators); #78 (PR D drift fix - a real correctness bug caught at cutover verification when a live new-hire submission produced ~226ms drift between Sheets col A and PG `created_at`/`submitted_at` because each adapter in the dual-write orchestrator independently called `new Date().toISOString()` and the wall-clock between the two sequential calls became a real timestamp drift in the two stores for the same logical event; fix stamps the event-moment ONCE at the orchestrator level before dispatching to both adapters, with optional 4th param + `|| new Date()` fallback in the adapter signatures for backward compatibility). After PR D deployed (20:53:05 UTC) the live byte-equal re-verification confirmed all writes match across stores; one pre-fix row carries the historical 226ms drift permanently with no token-math impact. Then `READ_FROM_POSTGRES_PEOPLE=submissions` + `READ_FROM_POSTGRES_DASHBOARD=submissions` flipped, verified visually (28 history items + 3 admin queue items for k.fietek). Three modules now live on Postgres (news_interactions / directory / submissions); 6 tables / ~189+ rows mirrored. Key lessons: (1) dual-write timestamp drift is its own correctness-bug class - orchestrators must coordinate event-moments before dispatching, never rely on per-adapter `new Date()` (caught empirically, ~226ms drift on a real production submission, blocking the read flip until fixed); (2) "long-term health, not shortcuts" framing applied four times today - dual-column timestamp split (PR A mid-build pivot from a single mutable `created_at` to the immutable-origin + mutable-last-touched pair), shared backfill runner extract over a standalone file (PR C, builds the engine while there are only 2 backfills to migrate not 7), admin_action_at preservation via sidecar read (PR C, reaching behind the canonical abstraction is acceptable when scoped to one place documented clearly), drift fix as PR D before cutover instead of after (don't ship the read flip with known drift on dual-writes); (3) verification-script discipline: always print the headers row alongside column data so labels are auditable - the phantom "13 -> 12 accounts drift" earlier in the session was caused by a verification script that mislabeled `r[1]` (name column) as `team_key` (which is actually `r[0]`), not real data drift. Open threads for tomorrow-Kevin: migrate `scripts/backfill-directory.mjs` to use the shared runner (cleanup PR, low priority), next module TBD (incidents structure-only EMPTY per audit / drafts + notification_log multi-writer / or jump to next migration-order module), drop submissions+directory+news_interactions from `DUAL_WRITE_TABLES` eventually (no urgency, rollback net stays on for weeks), e2e infrastructure fix (P2, overdue), sub-{N} token deprecation in favor of UUIDs (future PR after submissions stable).

2026-05-28 - Project 3 (finance stack) scoping day. Shipped 5 PRs: #80 (hero_images A1 header preservation - a self-perpetuating latent bug where the canonical reader was treating row 1 as headers on a tab that has no header, then the write path cleared A:A and wrote starting at A1, silently dropping the implicit A1 row on every replace cycle; fix preserves A1 as a stable header marker so the bug stops self-perpetuating without disturbing the existing data layout); #81 (migrated `scripts/backfill-directory.mjs` from its embedded runner to the shared `scripts/_lib/backfill-runner.mjs` extracted in PR #77 - bundled a small tightly-bounded runner fix for double-`--` in dry-run hints; closes backlog item (a) and validates the shared runner is now the single backfill engine for all current cutover modules); #82 (cutover.js doc accuracy fix - caught during Project 2 pre-flight before Kevin made the actual env change to decommission news_interactions from DUAL_WRITE_TABLES; verified empirically the existing code does NOT support that state - removing from DUAL_WRITE_TABLES stops PG writes not Sheets writes, producing the documented state-4 misconfiguration where PG goes stale on next user write; updated the doc block to match reality + flagged as future work; news_interactions decommission deferred until a real third flag or orchestrator semantic inversion is added); #83 (`docs/FINANCE_STACK_AUDIT.md`, 1294 lines / 10 sections, code audit of 26 files / ~16,400 LOC across vendor stack + invoice stack + Smart Inventory + cron repo + shared infrastructure; top finding was that `opsUtils.getAllVendors` + `resolveVendorId` carry a positional-index drift bug BUT have zero live callers - imported in `inventoryActions.js` but never invoked, downgraded from HIGH to LATENT severity after consumer-grep verification; captured `zone_corrections` tab live schema, 9 columns; verified the entire BR set and the live data shapes); #84 (`docs/FINANCE_STACK_PLAN.md`, 1087 lines / 9 sections, planning artifact built on the audit + Kevin's 17 locked decisions; 16 PG tables with full DDL including PG GENERATED columns + partial UNIQUE INDEX for conditional dedup + soft-delete `deleted_at TIMESTAMPTZ` + service-role-only RLS as a universal pattern; 12 PRs scoped across 5 modules + the kitchfix-inventory-cron cross-repo, 105-145 hours total estimate; sequential cutover with schema PR in parallel as the parallelism rule; Section 8 items all resolved - RLS service-role-only, primary_vendor log+null option (a), BR4 manual pre-backfill cleanup option (b), stub list confirmed, is_purchasing existing exclusions confirmed). Three modules still live on Postgres (news_interactions / directory / submissions); Project 3 ready for execution starting at PR 4.1 (dataStore.js split into module facades). Key lessons: (1) latent vs live bug categorization matters for severity grading - the opsUtils positional-drift call would absolutely cause vendor-resolution corruption if invoked, but it's never invoked, so it's a latent landmine not a live data corruption; flagged for cleanup, not P0 fix; (2) pre-flight verification catches architectural drift before it hits production - the news_interactions decommission would have created the silent state-4 data-loss state if Kevin had just set the env var without the pre-flight code check; PR #82 paid for itself before the actual change was even attempted; (3) doc/code drift is a real bug class - the cutover.js comment block was written assuming a future-state feature (final DUAL_WRITE_TABLES removal) that was never implemented, then it sat in the codebase as a misleading instruction for weeks until pre-flight surfaced it; (4) audit-then-plan two-PR sequence builds a verifiable scoping artifact rather than a hand-waved plan - the audit ground-truths the code reality (26 files / ~16,400 LOC, real BRs, real data shapes), the plan builds on that substrate, and Kevin's decisions get layered on top with explicit lock points so future-Kevin (and future-Claude) can re-read the plan and know what was decided vs what was open. Open threads for tomorrow-Kevin: start Project 3 execution at PR 4.1 (dataStore.js split), Module 4 (People Portal continuation - incidents / drafts + notification_log) still TBD vs jumping directly into Project 3 finance stack, e2e infrastructure fix (P2, overdue), sub-{N} token deprecation in favor of UUIDs (future PR after submissions stable). Mode = fast-as-safe held through the day - 5 PRs in one session is the highest-velocity day of the migration so far, supported by tight scoping discipline (each PR was one concern with explicit verification before merge) and the shared infrastructure now being load-bearing enough to compound on itself.

