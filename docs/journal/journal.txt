2026-05-27 - Module 3 (People Portal submissions) cutover. Shipped 4 PRs in one session: #75 (PR A dormant dataStore adapters + sub-{N} token translation + dual-column timestamp model `created_at` immutable / `submitted_at` mutable, with the dashboard cross-module read pre-wired so the dashboard cutover could sequence independently of the people module via `READ_FROM_POSTGRES_DASHBOARD`); #76 (PR B handler rewire across 8 sites in people/route.js - bootstrap counts, history, admin-queue, admin-queue-closed, submit-newhire, submit-paf, withdraw/cancel-submission, admin-process - all preserving pre-existing handler quirks faithfully per byte-identity contract; verified all 4 read sites byte-equal across 109 rows for k.fietek); #77 (PR C parameterized backfill of 109 historical rows including 106 with `admin_action_at` preserved via a sidecar raw col-K read with defensive `Date.parse` round-trip; extracted `scripts/_lib/backfill-runner.mjs` as the reusable engine for future per-module backfills with upsert / insert-if-empty / replace-null-pool strategies + sidecar reads + validators); #78 (PR D drift fix - a real correctness bug caught at cutover verification when a live new-hire submission produced ~226ms drift between Sheets col A and PG `created_at`/`submitted_at` because each adapter in the dual-write orchestrator independently called `new Date().toISOString()` and the wall-clock between the two sequential calls became a real timestamp drift in the two stores for the same logical event; fix stamps the event-moment ONCE at the orchestrator level before dispatching to both adapters, with optional 4th param + `|| new Date()` fallback in the adapter signatures for backward compatibility). After PR D deployed (20:53:05 UTC) the live byte-equal re-verification confirmed all writes match across stores; one pre-fix row carries the historical 226ms drift permanently with no token-math impact. Then `READ_FROM_POSTGRES_PEOPLE=submissions` + `READ_FROM_POSTGRES_DASHBOARD=submissions` flipped, verified visually (28 history items + 3 admin queue items for k.fietek). Three modules now live on Postgres (news_interactions / directory / submissions); 6 tables / ~189+ rows mirrored. Key lessons: (1) dual-write timestamp drift is its own correctness-bug class - orchestrators must coordinate event-moments before dispatching, never rely on per-adapter `new Date()` (caught empirically, ~226ms drift on a real production submission, blocking the read flip until fixed); (2) "long-term health, not shortcuts" framing applied four times today - dual-column timestamp split (PR A mid-build pivot from a single mutable `created_at` to the immutable-origin + mutable-last-touched pair), shared backfill runner extract over a standalone file (PR C, builds the engine while there are only 2 backfills to migrate not 7), admin_action_at preservation via sidecar read (PR C, reaching behind the canonical abstraction is acceptable when scoped to one place documented clearly), drift fix as PR D before cutover instead of after (don't ship the read flip with known drift on dual-writes); (3) verification-script discipline: always print the headers row alongside column data so labels are auditable - the phantom "13 -> 12 accounts drift" earlier in the session was caused by a verification script that mislabeled `r[1]` (name column) as `team_key` (which is actually `r[0]`), not real data drift. Open threads for tomorrow-Kevin: migrate `scripts/backfill-directory.mjs` to use the shared runner (cleanup PR, low priority), next module TBD (incidents structure-only EMPTY per audit / drafts + notification_log multi-writer / or jump to next migration-order module), drop submissions+directory+news_interactions from `DUAL_WRITE_TABLES` eventually (no urgency, rollback net stays on for weeks), e2e infrastructure fix (P2, overdue), sub-{N} token deprecation in favor of UUIDs (future PR after submissions stable).

2026-05-28 - Project 3 (finance stack) scoping day. Shipped 5 PRs: #80 (hero_images A1 header preservation - a self-perpetuating latent bug where the canonical reader was treating row 1 as headers on a tab that has no header, then the write path cleared A:A and wrote starting at A1, silently dropping the implicit A1 row on every replace cycle; fix preserves A1 as a stable header marker so the bug stops self-perpetuating without disturbing the existing data layout); #81 (migrated `scripts/backfill-directory.mjs` from its embedded runner to the shared `scripts/_lib/backfill-runner.mjs` extracted in PR #77 - bundled a small tightly-bounded runner fix for double-`--` in dry-run hints; closes backlog item (a) and validates the shared runner is now the single backfill engine for all current cutover modules); #82 (cutover.js doc accuracy fix - caught during Project 2 pre-flight before Kevin made the actual env change to decommission news_interactions from DUAL_WRITE_TABLES; verified empirically the existing code does NOT support that state - removing from DUAL_WRITE_TABLES stops PG writes not Sheets writes, producing the documented state-4 misconfiguration where PG goes stale on next user write; updated the doc block to match reality + flagged as future work; news_interactions decommission deferred until a real third flag or orchestrator semantic inversion is added); #83 (`docs/FINANCE_STACK_AUDIT.md`, 1294 lines / 10 sections, code audit of 26 files / ~16,400 LOC across vendor stack + invoice stack + Smart Inventory + cron repo + shared infrastructure; top finding was that `opsUtils.getAllVendors` + `resolveVendorId` carry a positional-index drift bug BUT have zero live callers - imported in `inventoryActions.js` but never invoked, downgraded from HIGH to LATENT severity after consumer-grep verification; captured `zone_corrections` tab live schema, 9 columns; verified the entire BR set and the live data shapes); #84 (`docs/FINANCE_STACK_PLAN.md`, 1087 lines / 9 sections, planning artifact built on the audit + Kevin's 17 locked decisions; 16 PG tables with full DDL including PG GENERATED columns + partial UNIQUE INDEX for conditional dedup + soft-delete `deleted_at TIMESTAMPTZ` + service-role-only RLS as a universal pattern; 12 PRs scoped across 5 modules + the kitchfix-inventory-cron cross-repo, 105-145 hours total estimate; sequential cutover with schema PR in parallel as the parallelism rule; Section 8 items all resolved - RLS service-role-only, primary_vendor log+null option (a), BR4 manual pre-backfill cleanup option (b), stub list confirmed, is_purchasing existing exclusions confirmed). Three modules still live on Postgres (news_interactions / directory / submissions); Project 3 ready for execution starting at PR 4.1 (dataStore.js split into module facades). Key lessons: (1) latent vs live bug categorization matters for severity grading - the opsUtils positional-drift call would absolutely cause vendor-resolution corruption if invoked, but it's never invoked, so it's a latent landmine not a live data corruption; flagged for cleanup, not P0 fix; (2) pre-flight verification catches architectural drift before it hits production - the news_interactions decommission would have created the silent state-4 data-loss state if Kevin had just set the env var without the pre-flight code check; PR #82 paid for itself before the actual change was even attempted; (3) doc/code drift is a real bug class - the cutover.js comment block was written assuming a future-state feature (final DUAL_WRITE_TABLES removal) that was never implemented, then it sat in the codebase as a misleading instruction for weeks until pre-flight surfaced it; (4) audit-then-plan two-PR sequence builds a verifiable scoping artifact rather than a hand-waved plan - the audit ground-truths the code reality (26 files / ~16,400 LOC, real BRs, real data shapes), the plan builds on that substrate, and Kevin's decisions get layered on top with explicit lock points so future-Kevin (and future-Claude) can re-read the plan and know what was decided vs what was open. Open threads for tomorrow-Kevin: start Project 3 execution at PR 4.1 (dataStore.js split), Module 4 (People Portal continuation - incidents / drafts + notification_log) still TBD vs jumping directly into Project 3 finance stack, e2e infrastructure fix (P2, overdue), sub-{N} token deprecation in favor of UUIDs (future PR after submissions stable). Mode = fast-as-safe held through the day - 5 PRs in one session is the highest-velocity day of the migration so far, supported by tight scoping discipline (each PR was one concern with explicit verification before merge) and the shared infrastructure now being load-bearing enough to compound on itself.

2026-05-29 - Highest-velocity day so far. 16 PRs across the arc from Project 3 Module 4 (#86 dataStore facade split) through Module 5 (Vendor) executing end-to-end and sealing, Module 6 (Invoice) prep landing in full, the `/docs` folder audit + cleanup, and the first architecture doc shipping. By area: Module 4 = #86 (dataStore.js split into module facades, +46 LOC net, 11-line shim retained because Node ESM does not auto-resolve `.js` paths to directories - a deviation from the original recon plan, kept because the empirical Node behavior demanded it). Module 5 (Vendor) execution arc: #87 (PR 5.1 PG schema + 8 dormant orchestrators + verify script + the GRANT-block discovery for Supabase project lacking default-privileges grant, plan amended) -> #88 (PR 5.2 handler rewire across 13 vendor sites in invoiceActions.js + S1/S2/D1/L8/L9 cleanups + BR1 admin gate on handleVendorMerge, -163 LOC net, added 9th + 10th orchestrators `getVendor` + `getVendorsForMatching` during the rewire when the bootstrap response contract revealed missing shape) -> #89 (PR 5.3 backfill 31 vendors / 56 vendor_accounts / 51 vendor_aliases + `merge_vendors` PL/pgSQL stored function with SECURITY INVOKER + GRANT EXECUTE, 17/17 verification PASSED including ON CONFLICT DO NOTHING via pre-seeded conflicting alias + rollback path via FK violation). Module 5 cutover then surfaced 3 mid-flight bugs all fixed before completion: (1) PR #92 fix - PR 5.2 missed propagating `module: "ops"` to the 5 vendor orchestrator call sites; without it, `isReadFromPostgres` always skipped the per-module check so `READ_FROM_POSTGRES_OPS` env var would have been a no-op at Step 7; caught mid-cutover during the recon for the env-var flip, not at the flip itself; shipped as 5 ins / 4 del fix preserving zero functional change pre-env-set. (2) Mid-debug discovery for dual-write appearing blocked - deployed PR #90 (boot log instrumentation) + PR #91 (6-point PG adapter logs) only to find PG writes WERE landing all along; the "0 rows" verify reports were correct readings of an actually-empty PG at those earlier moments. Whatever was blocking earlier writes resolved during the PR #90 deploy (probable cause: Vercel was serving warm Lambdas with stale module state from a prior failed deploy; PR #90's fresh build cold-started all Lambdas and flushed them). (3) Backfill data integrity - 2 duplicate `(vendor_id, account_key)` pairs in production Sheets blocked the upsert with PG error 21000; TRU-437/CIN-OH was a legacy double-insert (F19b bypass), SAM-470/STL-FL was a botched merge artifact (handleVendorMerge updates col B but not col A rowId leaving 2 rows pointing at the same key); both resolved via targeted Sheets cleanup. Module 5 (Vendor) sealed LIVE on Postgres 2026-05-29 with final counts 31 vendors / 54 vendor_accounts / 49 vendor_aliases (PG GENERATED alias_normalized column collapsed 2 case/punctuation dupes via UNIQUE - intentional). Debug instrumentation reverted in PR #93 immediately post-cutover (-44 LOC net), bundled with the dashboard sync that brought PR count 49 -> 58 + added rows for #85-#93 + captured the 3 cutover lessons in PR body + dashboard for future Module 6/7 reference. Module 6 (Invoice) prep then shipped in full: #94 (Module 6 data audit, 4 tables + 218 row-level historical artifacts catalogued + 5 issues found across 4 source tabs - CRITICAL header/data column mismatch in invoice_submissions_26, CRITICAL status column overload between AI scan status and workflow status, HIGH 209 orphan AI line item rows referencing non-existent invoice_uuids, MEDIUM 4 duplicate (invoice_uuid, line_num) pairs, LOW 5 unparseable invoice_date values; Section 8 locks the preservation-first architecture with `is_historical=TRUE` + `data_provenance` tagging on all 4 invoice tables and strict constraints conditional on `is_historical=FALSE` via partial indexes - the cleanup-pattern shift from "clean Sheets pre-migration" to "preserve drift + tag" that becomes Module 6+ standard). #95 (PR 6.1 PG schema + dormant adapters, 4 tables + 11 orchestrators + 8 indexes + the F25 client_uuid UNIQUE + F24 (vendor_id, invoice_number_normalized, invoice_date, total_amount) partial UNIQUE + ai_scan_complete GENERATED column with COALESCE for backwards-compatible NULL handling because the naive `ai_scan_status = 'complete'` produces NULL not FALSE under SQL three-valued logic; verify script 9 checks PASSED; behavior byte-equivalent with flags off). #96 (PR 6.2 handler rewire across 13 invoice-specific Sheet I/O sites in invoiceActions.js, 1689 -> ~1500 LOC, 9 cross-cutting cleanups bundled - BR1 admin gate on invoice-delete-dupe + S1 GL parsing helpers dropped + S2 parseSubmissionRow replaced with toLegacySubmission boundary translator + S3 normalizeInvNum local helpers dropped + S4 updateScanStatus stub replaced with markScanStatus -> updateInvoiceFields({aiScanStatus}) + D1 ensureLineItemTab moved into dataStore as Sheets-adapter internal + L1 "Invoice Uploads" fallback dropped + L2 LINE_ITEM_HEADERS moved + L3 unused Sheets imports pruned; orchestrator-level dedup signal `upsertInvoiceSubmission` now returns `{submission, deduplicated}` per locked decision C6; invoice-delete-dupe switched from hard row-delete to status='deleted' soft-delete; soft-delete filter added to invoice-history + invoice-admin-list to preserve pre-PR-6.2 UX; `module: "ops"` passed at every orchestrator call site per Module 5 lesson #1; behavior byte-equivalent with flags off). Doc work end-of-session: #97 (read-only `/docs` folder audit, 38 .md files categorized across CORE-ACTIVE / PROJECT-LIVING / MODULE-SPECIFIC / HISTORICAL / SUPERSEDED / PLACEHOLDER / UNCLEAR; 1 broken cross-reference surfaced - PROJECT_DASHBOARD references docs/DATA_MODEL.md which does not exist, actual data model split across SHEETS_AUDIT trio + FINANCE_STACK_PLAN + module READMEs; 4 open questions resolved by Kevin as ARCHIVE x 3 + KEEP x 1; architectural-doc gap analysis mapped source material into the 4 planned `docs/architecture/` files). #98 (cleanup execution per #97: 12 archive moves via `git mv` to `docs/archive/{handoffs,migration,specs}/` preserving full git history through `git log --follow`; one general-pattern lesson extracted from CLAUDE_KNOWLEDGE_MAP into GOTCHAS - "Positional-arg drift on shared helpers rots silently" generalizes the PR #59 wrong-arg-shape incident; broken DATA_MODEL.md TODO marked superseded; 6 active forward-pointing references updated to archive paths; 5 of 7 UPDATE-flagged docs deferred to Kevin as needing substantive judgment rather than mechanical date bumps; rebased onto merged PR #97 with single add/add conflict resolved by keeping the additive Consolidation Executed section). #99 (first doc under `docs/architecture/`, CUTOVER_PLAYBOOK.md 407 lines, distilled from cutover histories of Modules 1, 2, 3, and 5; 13-step cutover sequence with who-runs-it + how-to-verify + what-goes-wrong-if-skipped + time-estimate per step + the three Module 5 cutover lessons + audit-first principle + is_historical doctrine + 6 common pitfalls + module-specific quirks + cross-module dependencies; 1 of 4 planned architecture docs, the remaining three IS_HISTORICAL_PATTERN + PER_MODULE_FLAGS + IDEMPOTENCY_PATTERNS deferred until after PR 6.3 cutover provides the first live use of the patterns). Project 3 status end-of-day: 9 of 12 PRs done; Module 4 + Module 5 LIVE on Postgres; Module 6 ready-but-not-live (schema + adapters dormant, handlers wired, flags off); PR 6.3 (Module 6 backfill + cutover) is Monday's work and will be the first cutover executed step-by-step against the new playbook. Key lessons: (1) audit-first as a preventative response to the Module 5 cutover surprises - PR #94 is the templated implementation, and Kevin's resolution to preserve historical drift with is_historical + provenance tagging rather than pre-clean Sheets is the architectural shift that makes future cutovers deterministic. (2) Doc audit + cleanup pays for itself before the next cutover - the 38-file `/docs` corpus had accumulated 6 HANDOFFs + 3 superseded snapshots + 3 dead seed-corpus docs over a year of one-person-shop iteration; archiving them via `git mv` preserves history without cluttering, and the architectural-doc gap analysis surfaced source material that was distributed across 7+ docs so future architecture doc authors do not rediscover it from scratch. (3) The doc tiering policy (Tier 1 / Tier 2 / Tier 3) from PR #84 now in active use - PR #95 + PR #96 + PR #97 + PR #98 + PR #99 all self-declared their tier and the "Docs updated:" line in the PR description was a real artifact rather than aspirational. (4) Stacked PRs with explicit conflict-resolution plan work - PR #98 stacked on PR #97's audit doc, rebased after #97 merged with one clean add/add conflict resolution keeping the additive Consolidation Executed section. State going into Monday: branch on `main` (no in-flight branches), all PRs merged, no uncommitted work, no in-flight code reviews. Open thread = PR 6.3 (Module 6 invoice backfill + cutover) is Monday's first task. The CUTOVER_PLAYBOOK steps the execution: pre-flight verify -> the audit already done in PR #94 -> write `scripts/backfill-invoice.mjs` using the shared runner -> dry-run -> Kevin reviews -> live execute -> spot-check 3 representative records per table -> PR open + review + merge -> enable DUAL_WRITE_TABLES (append `invoice_submissions,invoice_rejections,ai_line_items,gl_codes`) + no-cache redeploy -> dual-write smoke test (invoice-submit / invoice-reject / invoice-unreject / invoice-dismiss-dupe / invoice-delete-dupe with soft-delete / triggerAIScan) -> append same 4 tabs to READ_FROM_POSTGRES_OPS + no-cache redeploy -> read-side smoke test -> 24-48 hour wait window -> document cutover complete. Mode held = fast-as-safe; today demonstrates the discipline scales when shared infrastructure (per-module flags, backfill runner, orchestrator-level coordination, doc tiering, audit-first) is load-bearing enough to compound. Backlog deferred from doc cleanup PR #98 (5 UPDATE-flagged docs needing substantive judgment): ARCHITECTURE.md five-pillar refresh post-modules-on-PG, CONVENTIONS.md captain's-log entries for orchestrator dispatch + module arg propagation, DESIGN_SYSTEM_REFERENCE.md navy `#153968` drift resolution, ENV_VARS.md Supabase-vars verification against Vercel state, RUNBOOK.md rollback + restore behavioral re-verify, modules/VENDOR_MODULE.md 4-section backfill to INVOICE_MODULE shape - tackle one per ~30 minute session when context allows.

2026-06-04 - Module 7 (Smart Inventory) closeout day. Three commits on feat/inv-3-backfill pushed to origin: 06005e7 backfill resume mode + price_history UNIQUE widening, 352f8f0 reconciliation alarm, 52d5da3 Sheets account canonicalization + dual-write E2E test. The arc: INV-3 backfill kicked off with full Phase 1-4 commits (32 storage_locations, 3759 inventory_items, 4342 item_aliases, 5 count_sessions, 147 count_items) then aborted at Phase 5 on a 23505 UNIQUE collision against price_history_item_id_source_or_invoice_id_key. Root-cause classification: 103 collision keys in Sheets price_history split 44 A1 (exact dupes, same recorded_at - a within-run cron loop bug, not cross-run idempotency) + 59 B1 (same (item, invoice, date) at different prices - real line-item-level data the cron writes legitimately). Decision: widen the UNIQUE key to (item_id, source_or_invoice_id, price) so B1 survives + dedup A1 in the backfill. Migration docs/migrations/inv-1-fix-price-history-unique.sql drops the old constraint and re-creates merge_inventory_items RPC with the matching NOT EXISTS clause; schema source updated in place. Backfill script gained --start-phase=N resume mode + integrity gate that asserts committed PG state matches recomputed maps exactly before Phase 5 fires + Phase 5 dedup on the widened key. Resumed run wrote 6665 price_history + 191 review_queue + 58 merge_history + 120 merge_history_items (1 approved orphan with item_id NULL + name snapshot). All FK + integrity spot-checks passed including the Samuels SAM-902 vendor mapping the typo "Samuels Seafoos" -> SAM-902 via vendor_aliases (seeded via scripts/_seed_samuels_alias.mjs, kept as provenance) and the B1 survival probe (two rows on the same invoice at different prices both landed). Then built scripts/reconciliation-alarm.mjs as a standalone node script (no src/lib imports, ports straight to the kitchfix-inventory-cron Railway repo) that compares Sheets and PG for every DUAL_WRITE_TABLES tab and posts a digest to SLACK_RECAP_WEBHOOK; 7-bucket classifier (reconciled, expected_cron_drift, structural_difference, known_residue, real_drift, quiet_window_skip, pg_only_reference) with 4 sheetsMode handlers covering real Sheets shapes that aren't 1:1 tab mappings (single, pg_only for vendor_aliases, embedded for invoice_rejections in cols R-U of invoice_submissions_26, per_account for ai_line_items / gl_codes fan-out). First clean baseline against the 13-tab pre-flip state caught two pre-existing drifts: vendor_master +6 (turned out to be the alarm's own tombstone filter checking the wrong column - actual Sheets soft-delete sentinel is col B name="" + col E notes="DELETED", not col B="DELETED") and invoice_submissions +7 (7 pre-cutover Sheets-only rows submitted against vendors later soft-deleted, intentionally Path-3-skipped by the PR 6.3 backfill; same shape as the documented SAM-956/38a56cc8 case). Both filter-fix + strictly-scoped residueCalculator landed in the same commit; clean baseline posted to #intranet-recap as 9 reconciled + 1 expected-drift + 1 known-residue + 1 structural + 0 gaps. Module 7 flip: 8 inventory tabs appended to DUAL_WRITE_TABLES (item_catalog, item_aliases, price_history, review_queue, storage_locations, count_sessions, count_items, merge_history), no-cache redeploy, alarm re-run with override showed all 8 in expected buckets (cron tables expected_cron_drift, intranet tables reconciled at drift 0). Test write via addStorageSubZone -> deactivateStorageLocation against storage_locations caught a content-level divergence the count-based alarm couldn't see: sort_order Sheets=0 vs PG=5 on a new sub-zone under STL-MO Walk-in Cooler. Root cause: 97 Sheets rows across 5 inventory tabs still carried the legacy long-form account labels ("STL - MO - St Louis Cardinals", "CIN - OH - Cincinnati Reds") because the INV-3 backfill canonicalized only the PG side; intranet's asymmetric Sheets-side accountMatch failed on short-query against long-row. Cron canonical-safety verified before fixing (kitchfix-inventory-cron/index.js line 47-51 uses a symmetric accountMatch that handles both directions, writes accountTab = AI_LINE_ITEMS tab title which is already canonical short). scripts/canonicalize-inventory-accounts.mjs (dry-run default, RAW batchUpdate, idempotent) applied the 2 mappings to 97 cells across storage_locations 32 + merge_history 58 + count_sessions 5 + item_catalog 1 + price_history 1; post-write re-scan confirmed 0 long-form remaining. Re-run of the dual-write E2E test produced sort_order 5=5 + identity on every other field (id, name, parent_location_id, account, active, color, icon) plus correct deactivation propagation (active=false, sort_order=999 both sides). Divergence closed. Test scaffolding fixed: scripts/_recon_test_dual_write.mjs Phase 6 hard-delete patched to resolve the numeric sheet gid via getSheetIdSA and pass a 0-indexed startIndex to deleteRowSA (the previous tab-name-string version had its API error swallowed by the SA helper's internal try/catch and silently left stray test rows). Final state: 21 tabs in DUAL_WRITE_TABLES with all 21 reconciling correctly per the alarm, reads still on Sheets (READ_FROM_POSTGRES untouched), cron untouched, alarm posting clean baselines to #intranet-recap. Key lessons: (1) D6 canonicalization needs to apply to BOTH sides at backfill time - INV-3 fixed PG via the CHECK constraint and assumed Sheets would self-canonicalize or stay tolerated by accountMatch, missing that the asymmetric tolerance is a one-way filter that silently breaks the other direction; (2) row-count reconciliation is structurally blind to content-level divergence - the alarm reported storage_locations reconciled at drift=0 even though every row had a different account label between stores; the content-spot-check (recent N rows, field-by-field) is the natural extension; (3) test scaffolding bugs hide behind try/catch swallowed errors - the test script's Phase 6 "OK" line was a lie for two runs before the missing gid arg was caught; helpers that catch + log + return success on error mask real failures. Open threads for tomorrow-Kevin: (a) docs/MODULE_7_DATA_AUDIT.md under-reported the long-form drift as 1 stray row when the actual scope was 97 across 5 tabs (storage_locations 32, merge_history 58, count_sessions 5, item_catalog 1, price_history 1); correct the doc so future readers have the right historical record. (b) Retire the asymmetric accountMatch in src/lib/dataStore/inventory.js (about 25 call sites) - both stores are canonical now so it reduces to strict equality; the helper plus all callers can be simplified, removing the asymmetry that caused this divergence class. (c) Add the reconciliation alarm's content-spot-check (deferred during the alarm build) - the recent-row field-by-field comparison would have caught the storage_locations divergence at the same moment the test write did, instead of needing a test write to surface it; pick the top-N-by-timestamp rows per side per dual-write table, confirm IDs reconcile, log any field-level mismatches. (d) Module 8 (cron -> PG) is the gate that unlocks inventory READ-cutover and chef go-live for Smart Inventory on Postgres; scoped work includes the ai_line_items.vendor_id FK addition, the cron col-N/timestamp fix that closes the col-N overloading residue currently quantified as known_residue by the alarm, and the Module 6 col-N overloading cleanup tracked separately under that arc. Mode = fast-as-safe held; today's lessons rolled into BUSINESS_NOTES/GOTCHAS deferred to a follow-up alongside the MODULE_7_DATA_AUDIT correction in (a). Post-closeout in the same session: scheduled the alarm on Railway as the tail of the 06:00 UTC cron run - same service via try/catch so an alarm failure does not change the cron exit code, with SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY + DUAL_WRITE_TABLES added as new env vars (GOOGLE_SERVICE_ACCOUNT_EMAIL + GOOGLE_PRIVATE_KEY + SLACK_RECAP_WEBHOOK reused). Intranet PR #117 refactored the alarm to export runReconciliationAlarm() so the cron repo could await import() it as a library; cron PR #3 then copied the alarm verbatim into the kitchfix-inventory-cron repo + added the try/catch tail call at the end of main(). The doc correction also landed: PR #123 corrected docs/MODULE_7_DATA_AUDIT.md to record the actual 97-row long-form-account drift scope instead of the original 1-stray-row claim, preserving the original wording scoped to item_catalog and adding a dated CORRECTION block that documents how the gap was found (the dual-write E2E test) rather than just patching the number - closing open thread (a). FIRST SCHEDULED 06:00 RUN exposed a bug in the alarm itself: the per_account read loop had a silent catch {} that swallowed transient Google Sheets API errors as zero-row reads, undercounting ai_line_items to Sheets 2965 vs the real ~7680 and firing a FALSE 🚨 REAL DRIFT (apparent +4610-row gap; real drift was the expected +105 cron-Sheets-only contribution). Verified via probe: PG 7575 (5842 historical from Module 6 backfill + 1733 live since cutover), Sheets actually 7680 across all 9 tabs all readable when re-tested, and the H3 dangerous-case check (recent PG writes without Sheets counterparts) was empty after correcting a UUID-join error - PG.ai_line_items.invoice_uuid is the PG submission id, not the Sheets client_uuid, so the comparison needs to translate through invoice_submissions before matching against Sheets col A (without that translation 500/500 recent PG rows looked Sheets-missing; with the correct join 500/500 reconciled). Same silent-swallow class as the test-script Phase 6 bug from earlier in the day (deleteRowSA's OK-line that was a lie because the SA helper swallowed the API error internally and returned success). Fixed with retry-with-backoff (2 retries at 500ms then 1500ms covers most transient blips) + fail-loud (any tab still failing after retries throws a descriptive error including lower-bound count + failed tab names + sample error; the throw bubbles to check1's outer try/catch and renders the table under ⚠️ CHECK ERRORS in the digest, NEVER silently treated as a real count that could trip drift classification). Landed in both repos byte-identical (SHA-1 d7312a89) - intranet PR #124 + cron PR #4 - both PR descriptions cross-reference each other and flag the duplication wart that bit twice today. Catch-scan of the rest of the alarm confirmed only the per_account swallow was dangerous: safeRead rethrows with tabname context, check1/check2 outer catches surface as visible error rows, postSlack is intentionally fire-and-forget per the cron/daily/route.js pattern, isDirectRun guard returns false on error as safe default. Manual re-run after the fix CONFIRMED the new behavior under stress: a repeated manual run hit Google Sheets rate limits again, but this time the alarm correctly reported 2 ⚠️ CHECK ERRORS (gl_codes 14 tabs + merge_history single tab) with 0 false drift, and ai_line_items correctly classified as expected_cron_drift +105 - the alarm is now strictly safer (can produce noise when a tab transiently fails, cannot produce wrong-but-quiet drift numbers). Key lesson today: empty catch blocks are a load-bearing failure mode in safety tooling and test scaffolding - two instances surfaced in one day (this + the deleteRowSA Phase 6 OK-line) argues for a CI grep or eslint rule flagging bare catch / catch (e) {} / catch () {} blocks in alarm + scaffolding paths so the next instance is caught at PR review. End-of-day clean-state on both repos: 0 open PRs (14 PRs merged today across the two repos), working trees clean (Category C/D untouched files left intentionally untracked as agreed), local main 1-3 commits behind origin/main only because main is checked out in another worktree (intranet) or needs a routine pull (cron) - origin/main fully current. Open threads updated, superseding the closeout-time (a)-(d) list earlier in this entry: (i) verify tomorrow's scheduled 06:00 UTC run lands as ✅ Dual-write recon clean - today's rate-limit errors were from repeated manual runs hammering the Sheets API and the single nightly tail call should not hit the same limit; (ii) tune alarm read-pacing for robustness under repeated invocations (longer backoff intervals, batching tabs across spreadsheet IDs, or inter-table sleep) so the alarm degrades more gracefully when someone runs it multiple times in quick succession during validation work; (iii) de-dupe-the-alarm-script follow-up is now HIGHER PRIORITY - same fix shipped twice in lockstep today (export refactor as PR #117 + cron #3, retry+fail-loud as PR #124 + cron #4) is concrete evidence that the two-repo duplication will keep biting until consolidated, candidate approach is publishing as a small private npm package or having the cron repo's CI fetch the intranet copy at build time; plus the existing follow-ups (the original (a) is now DONE via PR #123): (iv) retire the asymmetric accountMatch in src/lib/dataStore/inventory.js (~25 call sites) - both stores canonical now after the 97-row Sheets canonicalization, so the helper reduces to strict equality and the asymmetry that caused today's storage_locations sort_order divergence is eliminated structurally; (v) add the reconciliation alarm's content-spot-check (recent N rows field-by-field per dual-write table) - the deferred extension that would have caught the storage_locations sort_order divergence without needing a test write to surface it; (vi) Module 8 (cron → PG) is the gate that unlocks inventory READ-cutover + chef go-live, scoped work includes ai_line_items.vendor_id FK + cron col-N/timestamp fix + Module 6 col-N overloading cleanup.

2026-06-08 - Module 8 PRs 8.1 and 8.2 both sealed. PR 8.1 (PR #126, ai_line_items.vendor_id FK) executed as the two-part NOT NULL migration: Part A added the column nullable, the backfill populated existing rows via the lower(vendors.name) exact + vendor_aliases.alias_normalized fallback resolution, code merged confirming live writes carry vendor_id (Alsco rows verified at the first cutover-time invoice submission), one verify-script probe row cleaned (the line_num=9999 NULL/FAKE probe collision logged for a separate follow-up), then Part B applied SET NOT NULL + the FK constraint + the index; all 6 verification checks PASSED and the isolated FK probe returned the expected 23503 foreign_key_violation on a fabricated bad vendor_id, prod-verified that the FK is doing its job. PR 8.2 then shipped @kitchfix-intranet/shared@1.0.1 as a private GitHub Packages npm package, the dedup vehicle for the alarm + inventory-pg helpers that were duplicated across the intranet and kitchfix-inventory-cron repos. Scope was strictly additive: zero rewiring of live callers, the backfill kept its embedded helpers, invoice.js kept its inline vendor resolution, all 23 module orchestrators untouched, and both reconciliation-alarm copies left in place; the package only adds itself to each consumer's dependency tree so the install path is exercised end-to-end before any real consumer swaps. Package contents: src/inventory-pg/ exports loadVendorMaps + resolveVendorId + resolveVendorIdAuto + insertInventoryItems + insertItemAliases + insertPriceHistory (with a new onConflictDoNothing option flagged as a behavioral addition not present in the original backfill) + insertReviewQueue, all taking the supabase-js service-role client as the first argument so the helpers carry no env coupling or module-scope state; src/alarm/ holds a byte-identical port of scripts/reconciliation-alarm.mjs at SHA d7312a89 so the alarm hardening from PR #124 + cron #4 carries forward verbatim. Four verification gates closed sequentially over the session: (A) publish + read-auth, the publish PAT pushed v1.0.0 then v1.0.1 to GitHub Packages npm, the separate read PAT (read:packages only) authenticated successfully on a clean install test; (B) Vercel preview build + GitHub Actions npm ci, the first 1.0.0 build hit ERESOLVE because the package declared googleapis ^144.0.0 while the intranet pins 171.4.0, fixed by widening to googleapis >=144.0.0 and bumping to 1.0.1; on the second attempt Vercel passed clean (PR #127) but the e2e workflow then 401'd because the repo's .npmrc references ${NODE_AUTH_TOKEN} and the workflow had no env block setting it, fixed by adding NODE_AUTH_TOKEN as a GitHub Actions repo-level secret + a 3-line env block on the Install dependencies step (no permissions block, no setup-node registry-url, since those would conflict with the already-checked-in project .npmrc); (C) Railway install (cron PR #5), install-only branch on the cron repo added .npmrc + the dep line with no runtime import, Railway main-only auto-deploy on merge added 56 packages cleanly with no 401 and the image built and pushed; (D) import probe, a Node stdin module run from the intranet's cwd confirmed all 4 functions resolve correctly (runReconciliationAlarm, resolveVendorId, loadVendorMaps, insertInventoryItems all typeof "function"), with the inventory-pg side resolving against no env at all and the alarm side surfacing a latent design bug logged separately below. Durable infrastructure facts to remember: the read PAT named "kitchfix-shared-read-ci" (read:packages scope only) now lives in THREE places, Vercel (Prod + Preview + Dev environments), Railway (the cron service env), and GitHub Actions secrets on the intranet repo, all as NODE_AUTH_TOKEN; EXPIRES on 2026-09-03 (90 days from 2026-06-05), and WHEN IT EXPIRES all three break simultaneously (Vercel deploys + Railway deploys + intranet CI all 401 on npm ci/install), so rotation is a synchronized three-place lift; the publish PAT named "kitchfix-shared-publish" (write:packages + repo) is LOCAL ONLY in ~/.npmrc, never deployed anywhere, used only for npm publish from the kitchfix-shared repo; the consumer .npmrc (2-line, references ${NODE_AUTH_TOKEN} with no secret in the file itself) is committed in both intranet and cron repos with identical content; the KitchFix-Intranet account is a USER account not an organization which affects package permission semantics (cross-repo GITHUB_TOKEN reads on user-owned packages require explicit "Manage Actions access" linkage per package, which is why option B (a dedicated read PAT) was chosen over option A (GITHUB_TOKEN with permissions: packages: read) for the GH Actions fix). Open follow-ups, none blocking PR 8.3: (1) alarm lazy-init bug discovered by Verification D, the reconciliation-alarm.js module calls createClient(SUPABASE_URL, ...) at module top level (line 93), and the in-file comment claims createClient does not validate URL at construction time so a library import without env would not crash, but in supabase-js 2.45+ this is FALSE because validateSupabaseUrl runs synchronously and throws "supabaseUrl is required."; production paths have env set so the bug is latent (cron + intranet both work), but any test or local context without env crashes at import; fix during the eventual alarm de-dupe by moving createClient inside runReconciliationAlarm or behind a memoized getSupa() and correcting the comment, bump to 1.0.2 (or sooner if hit in a test context); (2) GH Actions Node 20 to 24, actions/checkout@v4 and actions/setup-node@v4 still run on Node 20, GitHub forces Node 24 default on 2026-06-16 (8 days from today), bump action versions before then; (3) de-dupe local copies into the shared package (PR 8.4 or later, optional, per-caller reversible), invoice.js inline vendor resolution about 10 LOC, both alarm copies about 929 LOC each which become thin shims, backfill Phase 2/3/5/6 inline helpers, take them one caller at a time so each migration is independently revertible; (4) duplication inventory, a read-only scan of both repos producing a prioritization MAP of what is duplicated vs what is genuinely shared-logic candidate, NOT a mass-refactor, future scoped task; (5) anti-duplication guardrail, the shared package is the home for genuinely-common logic especially infrastructure, extract at the ~third copy, do not pre-abstract (duplication is cheaper than the wrong abstraction); (6) verify-pr-8-1 script has two bugs logged at PR 8.1 close, (a) probe-isolation bug where both NULL and FAKE vendor_id probes used line_num=9999 and collided on the partial UNIQUE in the Part-A nullable-column state, fix is distinct line_nums, (b) distribution-panel pagination where the select("vendor_name, vendor_id") query capped at PostgREST default 1000 rows so vendor counts beyond 1000 silently truncate, fix is a .range() loop; (7) weekend alarm health check, glance #intranet-recap for the Friday + Saturday + Sunday 06:00 UTC runs to confirm the alarm produced clean 13-tab digests without false drift, the durability proof of the PR #124 retry + fail-loud fix from 2026-06-04. Next-session first moves: pull all three repos (intranet, kitchfix-inventory-cron, kitchfix-shared) to local main; then start PR 8.3, the cron-to-PG migration, which is the first real consumer of the shared package and the highest-stakes change in Module 8 since it touches real cron writes, migrate the cron's 4 Sheets write paths (item_catalog, item_aliases, price_history, review_queue) to PG via the shared inventory-pg helpers behind a CRON_USE_POSTGRES flag, dry-run-first with side-by-side validation against the existing Sheets writes, pre-cutover backup, and recon-first sequencing per fast-as-safe mode; this PR also folds in the col-Q overloading auto-fix because PG has separate status + updated_at columns while the Sheets schema overloads them into col Q. Post-closeout same-day, the inventory cron's 06:00 UTC manual re-run after Claude credit restore (299 items processed, 0 Claude API errors, image built + pushed) surfaced that the credit-outage window 2026-06-02 through 2026-06-06 had stranded 12 invoices (~$18K combined value) in the "silent gap" state, ai_scan_complete=TRUE but zero ai_line_items, because extractAndStoreLineItems at src/lib/invoiceActions.js:1337 was calling markScanStatus('complete') unconditionally at the bottom of its try block even on three documented failure paths: (D) parsed.lineItems > 0 but metadata.account missing meant warn + no insert + fall through to 'complete'; (F) insertAILineItems throws got caught and swallowed by the inner try/catch + fall through; (G) parsed.lineItems empty meant the insert path was skipped entirely + fall through. The reconciliation alarm's check2() correctly caught the resulting class as a 12-entry digest (Slack-truncated to 10 + "...2 more" per its built-in slice). DECISION on the 12 stranded invoices: WRITTEN OFF, consciously not recovered, because (a) re-extracting against the same Drive images that produced an empty or insertable-but-failed Claude response the first time will likely produce the same result on a second pass (the model is the variable, and we have no reason to expect a different answer from identical inputs), (b) the operator-side workflow has already moved past those dates and the missing line items are not blocking current ops, (c) the $18K combined value is roughly the cost of one operator-hour of investigation + remediation noise that would have to be coordinated through the AI extraction path plus the cron's catalog-matching downstream paths. Recoverable via scripts/_rescan_silent_gap.mjs (dry-run by default, --execute required for writes, pre-flight asserts PG=0 AND Sheets=0 before re-extracting any single uuid, post-flight re-counts both sides + prints final ai_scan_status) if we change our mind on a per-invoice basis. PR #129 (fix/ai-scan-status-conditional-complete) shipped the root-cause fix the same session: 3 bug paths D/F/G now mark 'failed' and return early; markScanStatus('complete') is reachable only after insertAILineItems is called and succeeds with > 0 rows. Diff +48/-33 LOC in one file (src/lib/invoiceActions.js); e2e workflow PASSED on the PR (Playwright install step authenticated against the private package via the NODE_AUTH_TOKEN GH Actions secret that PR #127 wired up; tests passed against prod URL); Vercel preview deploy SUCCESS; merged to main without a synthetic preview test because the structural review of all 7 paths (A-G) + the invariant "complete iff insertAILineItems succeeded" was sufficient. Post-merge verification deferred to the next real operator invoice via the saved scripts/_probe_invoice_status.mjs probe (single-uuid status + line item count classifier, healthy / silent-gap / failed / pending). HIGH-PRIORITY do-next followup, NOT BACKLOG: add a "failed-status invoices in lookback window" check to scripts/reconciliation-alarm.mjs as its own digest section so failed extractions are visible end-to-end, because the PR #129 fix trades silent-gap-class invoices (alarm-catchable) for loud-status-but-no-actor 'failed' invoices that no current consumer reads (no UI badge since the InvoiceTool badge renders only on 'complete' or 'pending'; no alarm check today; no script auto-picks-up; backfill-stl-mo's filter is ai_scan_status IS NULL OR = 'photo-only', does NOT include 'failed'). Without the alarm extension, this fix has only MOVED the silence, not eliminated it. Candidate to fold into PR 8.3's alarm work since that touches the alarm anyway. Three small probe/recovery scripts saved untracked for the day for reuse: scripts/_probe_silent_gaps.mjs (the full untruncated silent-gap list, alarm's check2 mirror + Sheets-side count + canary candidate filter, overridable lookback window), scripts/_rescan_silent_gap.mjs (the one-uuid canary, dry-run by default), scripts/_probe_invoice_status.mjs (the post-submit single-uuid health check). Discovered along the way and worth knowing for future migration work: the PG ai_line_items table's partial UNIQUE INDEX on (invoice_uuid, line_num) WHERE is_historical=FALSE is what makes re-extraction safe on a 0-row invoice (nothing to conflict with) AND what would 23505 a re-extraction on an existing-line-items invoice; the Sheets side has NO such dedup (insertAILineItemsSheets is a plain appendRowsSA), so any rescan tool must verify Sheets=0 too before re-extracting or it silently duplicates Sheets rows. Closing position end-of-day: 3 modules merged to main today (#128 journal, #129 silent-failure fix, and earlier #127 + #128 dependency tracking), 0 open PRs, working trees clean, PR 8.3 (cron-to-PG) recon kicked off after the journal closeout with the cron's 4 Sheets write paths mapped + the flag-gated CRON_USE_POSTGRES design proposed for review.
