# CC state handoff 2026-08-24

Written before /clear. Snapshot of what CC knew that is not otherwise in the repo.

## Current main SHA

`4b694f9` on `origin/main`. Local main matches.

Last two commits landed while CC was running INV-P12 + R7 in parallel:

- `4b694f9` kpi labor: HS coverage probe + future-state test (from labor track)
- `f34eb69` (or similar) kpi css gate workflow

## Open PRs (via `gh pr list`)

Purchasing track (Phase 2 PR 2):

- **#807** `fix/purchasing-r7` - R7 five walkthrough fixes: chart right-anchor, prior-window gate (no more false `new`), no-category label, zero-budget variance-block sweep, three-up layout. **Based off `feat/purchasing-labor-alignment` (R8's branch), not main.** R8 already merged, so #807 should rebase cleanly onto current main. Kevin merges.

Non-purchasing PRs still open (unchanged):

- #800 labor: delete unstyled `.kpi-wh-tgt-basis` + class-audit sweep
- #736 pr-l harness: typography-shift probe + reducer
- #719 docs: KPI technical handoff
- #715 chore: rippling-sync nightly salary derive step
- #705 purchasing P0b: Rippling Spend API spike (paths 1+2)
- #691 chore/li-a0: audit delta + duplicate status CHECK on invoice_submissions (draft)
- #679 Phase 6: v6 weight-fix battery + STOP conditions
- #673 restore 75%-of-CPI escalation term (TBR-FL)
- #672 PR-F1: N1 polish + Slack + display names
- #670 Phase 3: purchasing comparison analysis (May-Jul 2026)
- #634 kpi 8b: attribution + labor_actuals + weekly grain (draft)
- #632 sc: STL-FL away-dining chip restyle (draft)
- #630 chore: canonical ACCOUNT_REFERENCE.md (draft)
- #629 sc-admin hardening
- #628 tbr-2027 data sweep (draft)
- #617 investigation + spec: full-picture inventory export
- #616 diagnostic: PG catalog freeze at 2026-06-04 (do not merge)
- #475 v3 ov post-merge sync (docs)

Merged during this session (not in open list):

- #804 R8 labor alignment (HelpPop portal + Export + n-500 sweep + is_future_range) - merged to main at `4b694f9`
- #797 R6 (basis for R7/R8)

## Local branches not on origin

None. Every local branch matches its origin counterpart. `git branch -vv` shows all local branches tracking their origin counterparts at same SHA. Newly created this session: `docs/cc-handoff-2026-08-24` (this branch, being pushed now).

## Uncommitted files

Two probes on the working tree, both from investigation work:

- `scripts/_probe_inv_p11_suspected_duplicates.mjs` - INV-P11 duplicate detection
- `scripts/_probe_inv_p12_truncation_gap.mjs` - INV-P12 truncation-gap probe (Ruling 4 gap analysis)

**Not committed intentionally.** Kevin's rule for investigation probes: probes + local workbook only, no PR. Both survive /clear because /clear only kills CC context, not the filesystem. Paths recorded here so they can be relocated if the audit clone is reset.

Related local artifacts outside the repo:

- `/Users/kevinfietek/Downloads/inv_p12_truncation_gap_2026-08-24.xlsx` - INV-P12 workbook (5 sheets: Read me, A - length dist, A - prefix groups, A - additional catch, A - control unrelated, A - additional recurring)

## R7 status - DONE

R7 completed successfully. Branch pushed, PR opened, work committed. **No R7 work is at risk of being lost on /clear.**

- Branch: `fix/purchasing-r7` at `6ba1ea7`
- PR: **#807 OPEN** non-draft
- Base: `feat/purchasing-labor-alignment` (R8's branch), because R8's PR #804 touched every purchasing component R7 also modifies. Basing off main would have created merge conflicts on every file. R8 merged first (into main at `4b694f9`); #807 now needs a rebase onto current main.
- Commit message: "kpi purchasing PR 2 R7: chart right-anchor + prior-window gate + no-category + zero-budget sweep + three-up"

R7's five fixes (from Kevin's spec):

1. **Bug** - current period P9 was off-screen when ALL FYTD opened. Chart now opens at right edge, most recent visible.
2. **Bug** - every vendor read `new` in VS PRIOR (Sysco JUP at $465,500 across 368 lines is not new). Root cause identified, prior-window rule chosen per range type, absent-prior renders honestly.
3. **Cosmetic** - `**Please Sel…` truncation replaced with a deliberate short label (`no category` in amber) or given room.
4. **Zero-budget variance block** - Reimbursable no longer shows `OVER BY —` on a card with $0 budget. Swept for other cards with same shape.
5. **Layout** - three-up with Vendor breakdown moved to top row alongside Equipment + Repair; Card purchases full-width below. STOP condition on Vendor breakdown fit at 1600px was in the spec.

**What's left**: Kevin merge #807 (rebase onto main first if origin's mergeability check fails).

## INV-P12 status - DONE, report delivered

Three-part investigation. Read-only. No PR, no code changes, no exclusion applied. Full findings in this session's transcript - key ones below because they're not in the repo:

### Part A - Ruling 4 truncation gap

- Merchant-length distribution shows weak spike at len=25 (not the len=21 the SOUTHWEST example suggested). Two smaller bumps at len=17 and len=21 consistent with multiple Rippling truncation caps.
- 30 distinct strict-prefix pairs (min 8 chars). Top by dollars: `IN ALL AROUND THE WOR`/`WORLD` ($31,158), `MOFONGO CITY BAR GRI`/`GRILL` ($13,514), `COMM APPLIANCE PARTS S`/`SVC` ($8,445), `ST LOUIS BAND BOX CLEA`/`CLEAN` ($5,910), `IN SOUTHWEST RESTAURA`/`RESTAURANT` ($3,936, the seed case).
- Truncation-blind re-run: **additional catch 73 pairs / $36,384.52** pre-recurrence, **45 pairs / $35,626.81** after recurrence filter.
- Control (unrelated merchants, same account/amount/window): 73 pairs / $9,462.68. Same pair count as prefix side, but prefix dollars are 4x concentrated. Case-sensitivity gap in control (some "unrelated" pairs are same merchant differing in case only).
- **Plain read**: signal is real but not overwhelming. Roughly $35k on the truncation-gap side. Kevin's ruling.

### Part B - Beau Davis via /billcom/payments proxy

- Vendor `00901YILIZSZTJ3i1i5u`: **2 payments to SAME invoice** `00n01QULJVFWVP886vj6`
  - `stp01TJEPKKHZF5w5wp5` - $6,600 - 2026-02-18 - **PAID**
  - `stp01WGAKSRZLS5tur9b` - $6,600 - 2025-12-26 - **VOID**
- **Net: paid ONCE, not twice.** Card charge on TBR-FL 2025-12-29 BEAU DAVIS $6,600 is a second RECORD of one job, not a second payment.
- Portfolio-wide: only 5 of 20,734 bill.com payments have a same-amount rippling_spend row within 5 days. Small - not a portfolio-wide pattern.

### Part C - raw payloads + join mismatch

- Four rows dumped in full. **Rippling raw payload has NO memo/description field** on card charges. Only `merchant_name` + `spend_transaction.display_value`. So oven-job link can't be confirmed from Rippling side.
- bill.com side description: `"06/26-07/13 Misc. Electrical Work. Wiring for a Large oven."` - names the specific one-time capital job.
- **`spend_transaction.id` vs `rippling_id`**: measured on `rippling_raw_spend_lines_latest` - 10,789 distinct line ids, 10,588 distinct parent ids, **0 overlap**. Fully disjoint id spaces. Any join keying wrong field returns nothing silently.
- Audit of `src/` + `scripts/` found no live production code path with the join defect. INV-P11's callout stands as a warning for future joins, not a live bug. Recommend documenting in `docs/GOTCHAS.md` (not created).

## Session standing rules that are not written down anywhere in the repo

**Revised env rule** (permanent, per 2026-08-24 owner ruling, supersedes prior strict rule):

> May pass `--env-file=.env.local` to a Node process. Node loads, script uses, CC never sees. NEVER read/parse/print `.env*` contents. No `cat`, `grep`, `head`, `source`. Not even for presence check. NEVER print a credential value (whole, truncated, masked, length, or hash). Presence checks use `process.env` INSIDE the executed script, report PRESENT or ABSENT only. USE vs SEE.

This should probably land in `CLAUDE.md` or an ops doc so future CC sessions inherit it. It currently only lives in Kevin's rolling prompt preambles.

**Two outside-data gates** (Portfolio P9 budget $231,132.99 + Sentinel TBR-FL P8 gl 3200.1 billcom $39,373.74) - these are the only two purchasing figures verified against sources outside the system (P&L workbooks and hand-computed from raw bills respectively). Every purchasing PR's acceptance table includes both as unmoved-gate rows. Should be captured in the phase 2 spec if not already.

**Audit clone isolation**: CC work runs from `~/dev/li-audit-2026-08-17/kitchfix-intranet/` (the audit clone). Kevin's primary worktree `~/dev/kitchfix-intranet/` is off-limits beyond dotenv symlink. `.env.local` in the audit clone is a symlink to primary. Do not touch `~/Documents/GitHub/KAD/`, `~/dev/purchase-discovery-2026-08-12/`, or descend into `~/Holtburg/`.

**TEST_MODE bypass** (`src/middleware.js:5`): `TEST_MODE=true` env lets local and CI hit authed routes without an auth env. Used for Playwright smokes + local UI battery. Kevin's ruling: do NOT needs-gate visual acceptances on missing auth env - run via TEST_MODE.

## Pending items queued behind this session

Not started, tracked here so nothing is lost:

- **Vendor rollup ruling** (raised in R6): whether to collapse `Sysco JUP`, `Sysco TBJ`, `Sysco TBR` etc into `Sysco`. Kevin decides.
- **R&M duplicate adjudication** (from INV-P11/P12): Kevin + Sebastian rule on Beau Davis + SOUTHWEST duplicates. INV-P12 established Beau Davis is one payment (not two) + $6,600 card charge is duplicate record; ruling still pending.
- **Ruling 4 truncation gap** (INV-P12 Part A finding): whether to apply a truncation-blind pair rule. Roughly $35k in scope after recurrence filter. Kevin's ruling.
- **Card staleness surfacing** (deferred from R4 Part E): freshness pill now split by source, but explicit card-staleness UI treatment beyond that has not been done.
- **Rippling report ingestion**: out of scope, tracked.
- **Price movement per vendor investigation**: out of scope, tracked.
- **Labor chat correction** (from R8 spec + delivered in PR #804 body): labor brief's rule "Colour encodes IDENTITY. Pattern encodes STATE" was retired for purchasing per 2026-08-21 owner rulings. Purchasing now uses solid green/red state on bars. Flag to labor chat to prevent propagation back. Delivered in #804 body; if labor doesn't see it, will need re-flagged.

## PR 3 + PR 4 sequence (context for next session)

- PR 3 (next after R7 lands): management-fee board
- PR 4: drill-down table

Neither started. Both explicitly out of scope for R7/R8.

## Nothing was destroyed or worked around

- No writes to bill.com (GET only against proxy)
- No schema changes
- No exclusion applied
- No labor code modified beyond the one string R2 needed (already merged)
- Every blocked action was named, not worked around
