# HANDOFF — 2026-05-13 — Phase 1 push day

## Current state
- Local main: `5a2650f` (after PR #13 docs merge)
- On branch: `feat/backup-sheets-cron` with uncommitted work in flight
- Untested code: `src/app/api/cron/backup-sheets/route.js` (138 lines)
- Modified: `vercel.json` (added 6th cron at `0 6 * * *`), `.env.local` (added `BACKUP_FOLDER_ID=1-Gedxfa0-e0FT6b562qx4Z_fIkj1oQtI`)

## Secrets/env set today
- Vercel env: `BACKUP_FOLDER_ID`, rotated `CRON_SECRET`
- GitHub secrets: `PLAYWRIGHT_AUTH_STATE_B64`, `VERCEL_AUTOMATION_BYPASS_SECRET`

## Shipped today (4 PRs merged)
- #10 — `logHealthSA` cleanup
- #11 — Playwright CI workflow (tests run against prod on every PR)
- #12 — Flexible auth.setup.ts URL regex
- #13 — Docs (TESTING.md + GOTCHAS.md updates)
- (no PR) — `CRON_SECRET` rotated, Vercel bypass token stored

## NEXT STEP — Step 4.4 — Manual curl test

Two terminals:

Terminal 1:
\`\`\`
npm run dev
\`\`\`
Wait for "Ready".

Terminal 2:
\`\`\`
curl -X GET http://localhost:3000/api/cron/backup-sheets \
  -H "Authorization: Bearer $(grep '^CRON_SECRET' .env.local | cut -d'=' -f2)" \
  -w "\nHTTP %{http_code}\n" \
  -s
\`\`\`

Expected: HTTP 200, success:true, 5 results, 0 errors. Takes 15-30 seconds. 5 dated backup files appear in Drive folder `1-Gedxfa0-e0FT6b562qx4Z_fIkj1oQtI`. Slack ping lands via `SLACK_RECAP_WEBHOOK`.

## After 4.4 works

1. Commit + push branch + open PR titled "feat: add daily Sheets backup cron"
2. Wait for CI green
3. Squash and merge
4. Verify Vercel cron registered (Settings → Cron Jobs, 6 entries)
5. Document restore procedure in `docs/RUNBOOK.md`

## Block 5 — after Block 4

Dependency pinning (~45 min). Audit package.json, pin googleapis/next/react/anthropic-sdk, verify build + tests still pass.

## Followups parked
- `SHEET_IDS.COLLECTION` hardcoded in `src/lib/sheets.js`, same ID as `PEOPLE_DB_SHEET_ID` env var — code cleanup later
- Backup retention policy — none today, add ~90-day sub-cron later
- Anthropic SDK consolidation (Task 4) — needs spec first
- Auth boundary cleanup (Task 9) + OAuth scope reduction (Task 10) — need quiet Saturday
- Observability (Task 3) — needs scoping session
- Next.js 16 middleware → proxy migration

## Key decisions today
1. CI runs against production, not preview URLs (bypass-token plumbing doesn't work through wait-for-vercel-preview)
2. Auth state is environment-scoped — regenerate against target env when switching
3. Backups skip ANALYTICS (generated data, cell-quota concern)
4. Backup pings reuse `SLACK_RECAP_WEBHOOK`

## Style preferences for next-Claude
- Step-by-step zsh, not VS Code clicks (except for sprawling edits)
- Before/after context blocks when editing code
- Surgical patches over full file replacements
- Terse confirmations from me ("ok", "do it") mean act on recommendations
- Honest pushback preferred over agreement